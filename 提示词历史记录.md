# 提示词历史记录 (Prompt History)

本项目中所有面向 AI 大语言模型 (Gemini-3.5-flash) 进行食堂备餐审计和膳食、采购、成本估算所使用的提示词全部在此处进行物理隔离备档，方便未来的开发和运维人员直接修改提示词以影响行为。

---

## 📅 初始化 V1.0.0 版本所采用的结构化提示词明细

以下是 2026-06-23 阶段，系统后端统一配置、定载在 `src/prompts.ts` 内的结构化提示词。

### 1. 系统角色定位提示词 (System Instruction)

```markdown
# 角色定义
你是一位拥有20年从业经验的“大型校园与机关食堂高级膳食营养调配师 & 财务精细化控制审计专家”。你的专业领域是针对幼儿、中小学生和成人在中式快餐、营养配餐、采购物流管理及控本、卫生安全规范上的深度审计。

# 任务目标
通过阅读用户提供的当月备菜记录（包括各品类备菜名称、单位、月成交数量、总单价、累计总金额等核心矩阵信息），为食堂出具极具专业可操作性的“膳食平衡、营养达标、财务精细化降本增效、仓储防损安全”深度审计报告。

# 处理准则
1. 你的分析必须严谨、客观，不罗列废话。
2. 结合人群属性：幼儿备餐重视细致易消化、少油盐；教师备餐重视控脂抗氧化、膳食平衡；低年级重视促进长高、补钙；高年级重视蛋白质与高强度能量供给。
3. 成本分析需要能够看出哪类食材是当前的开支大头，如何寻找低成本均衡替代品（如大豆腐、菌菇等）。
4. 必须依据用户指定的 [返回数据模式] 返回严格合规的 Markdown。
```

### 2. 备餐月度报表诊断审计的结构化提示词模板 (Prompt Template)

当用户点击“运行 AI 智能备菜审计与配餐预测”时，系统会在后端利用此模板整合当前报表汇总变量：

```markdown
# 备餐数据精细化审计契约

## 1. 待评估维度的当前背景
- **适用备餐人群**: {targetGroupLabel}
- **统计报告区间**: {year} 年 {month} 月
- **日度数据合计摘要**: {summarizedSummary}

## 2. 统计原始明细矩阵
以下是当前系统用户已录入的数据集（按主大类聚合的总数量与总开销）：
```json
{itemsJson}
```

## 3. 分析与评估维度 (Markdown结构化)
请对此数据集进行严格评判，并完成以下任务：
- **[膳食结构与营养平衡]**: 评估是否满足该段人群（如幼儿、教师或中小学生）的营养元素多样化需求，蔬菜、肉类、水果等比例是否健康，是否存在重肉轻菜、精制碳水过多问题。
- **[食堂成本与开支审计]**: 确定各品类中开销前列的“成本高位项目”，给出下月替代调优、节约能耗或减少低耗品的科学控本路径。
- **[智能采购与下步计划]**: 列出后续重点采购节奏，特别是对保质期短的叶菜类、肉类的冷链储存建议和定量指导。
- **[膳食平衡评估指数]**: 综合膳食丰富度、蛋白质充足度、开支合理性，给予一个 0 到 100 之间的最终平衡评价得分。

---

## 4. 返回要求 (Markdown结构化 & 精准JSON格式)
为了方便系统后台高吞吐量解析并进行渲染展示，请你严格以下方的 Markdown 结构格式进行输出，**不要**包含任何其他的问候和多余废话：

```json
{
  "nutritionAnalysis": "这里写详细的膳食结构与营养平衡诊断详情...",
  "costControlSuggestions": "这里写详细的食堂成本管控与降本增效路线详情...",
  "purchaseGuide": "这里写对存储冷链及后续重点采购流向的计划指导...",
  "healthScore": 85
}
```

请注意：`healthScore` 必须为一个 0~100 范围内的数字。请确保拼写正确并能成功调用 JSON.parse!
```

---

## 🔄 每次功能变更和微调的补录追记 (Append Only)

*(未来如有大语言模型的角色和提示词修改，在此分界线下方追加写入即可，请不要覆盖上面已有的历史记录。)*

### 🚀 2026-06-23 - [V1.1.0] 高密度翡翠绿（High Density）主题重塑
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：在本次高密度视觉主题和双栏解耦重构中，前后台接口以及 AI 智能备菜审计的输入输出格式契约完全保持兼容。定载在 `src/prompts.ts` 内的前后端流式 JSON 分析审计提示词未受到任何颠覆或修改，双核审计通信依旧稳健。

### 🚀 2026-06-23 - [V1.2.0] 行政配置管理后台（一级人设客群/二级食材大类增删改查）
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：在本次增加详细的后台管理系统、允许主管在线自由增删改查一级餐卡客群、二级餐饮原料品类、以及转移系统性性能和进程通信日志到后台查看的开发阶段中，前端和后端的数据契约和通信依旧完全依赖于底层的 json 化协议及事件流。由于我们在代码中对新增的属性、动态删除客群/品类做了极为健全的防御性自适应重置与核查，`src/prompts.ts` 中针对月度备餐大表而定制的 AI 智能系统角色、健康度得分以及 Markdown 返回参数模型格式契约依然完全兼容，不产生任何物理破坏。性能卓越。

### 🚀 2026-06-23 - [V1.3.0] 密码进入、全量一键式归核备份与汇总CSV导出
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：在本次完成进入管理后台口令（验证密码 admin）、支持导出备份 JSON 数据、导入备份 JSON、清空当月录入、出厂初始化演示，以及添加强大的全人群多维大宽表 CSV 汇总导出的开发阶段中，整体的前后台 AI 突触依旧稳健。前端的交互和密码遮罩层完全在客户端闭环安全，因此没有对 `src/prompts.ts` 的结构化诊断系统角色和提示词内容做任何物理修改，百分百向下兼容，性能无阻隔。

### 🚀 2026-06-23 - [V1.3.1] 单元格及新增项响应性物理 Bug 攻坚战
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：在本次修复“矩阵与单日卡片数值无法输入修改”以及“点击新增项无反应”的问题中，属于纯粹的 React 受控组件渲染依赖更新问题。所有的更新已经转为不可变数据机制。对于 AI 主管和专家系统提示词，未发生任何改变，完美的 100% 绿色兼容。

### 🚀 2026-06-23 - [V1.4.0] 首页安全解锁与多维密码 `.env` 高密化物理离析
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代中，我们彻底贯彻系统级合规物理隔离，引入了主首页登录屏机制，防止任何人非授权窥视食堂数据底盘。同时，将全部登录口令（首页系统密码、后台配置密码）平移注入根目录 `.env` 环境变量存储中。这一改进完全属于前端的顶层登录态管理与环境变量对接模块，对位于 `src/prompts.ts` 的专家审计分析系统角色及提示词大纲未引入任何破损，百分百无损级兼容，系统极速畅快。

### 🚀 2026-06-23 - [V1.5.0] 高性能审计日志体系与中文级联翻译转换
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代中，我们引入了高性能失去焦点(`onBlur`)物理触发的单格记账审计日志、食材品类CRUD与批量克隆审计日志等操作记录模块。虽然引入了深度的财务记账行为审计、操作员角色识别以及变动差额核查，但这完全属于前端 UI 交互与本地 `LogBroker` 日志总线服务架构，完全未改动位于 `src/prompts.ts` 内的 AI 审计人设提示词和结构化模板，因此 AI 模块 100% 保持无缝绿灯兼容。

### 🚀 2026-06-23 - [V1.6.0] 每日就餐人数录入与多合一全客群人均餐费走势分析
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：在本次新增每日就餐人数卡片网格以及全人群多色人均餐费分析折线图的开发阶段，底层的 AI 分析及专家系统角色设定（定载在 `src/prompts.ts` 中）仍旧完全适用并保持完美的多核兼容。

### 🚀 2026-06-23 - [V1.7.0] 就餐人数与人均餐费多维度分析隔离为独立顶级菜单栏入口
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：在本次独立分析页头变形、Aside 主菜单极速挂接、以及 `HeadcountPanel` 内部加装药丸式微切换 Tabs 的重构工作中，所有的功能都是基于 React 状态与 UI 结构流转完成。由于食堂决策分析被完全独立并纯净化一屏渲染，分析底座更稳健，提示词仍采用原中文化内置大纲，完美向下兼容。

### 🚀 2026-06-23 - [V1.8.0] 移动/平板端响应式适配与生命周期逻辑自愈
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：在本次针对手机端与 iPad 端的自适应汉堡包菜单、抽屉式侧边栏、Excel粘性锁定列，以及 App.tsx 数据状态恢复的自愈重构中，前后台接口数据流及 AI 智能对话模型提示词（`src/prompts.ts`）完全契合设计协议，没有任何结构性改变。100% 保持绿色兼容。

### 🚀 2026-06-23 - [V1.9.0] 管理后台侧边菜单面板重构与 Iframe 安全弹窗拦截自愈
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：在本次将管理后台拆分为侧边栏菜单（受众人群管理、大类管理、数据维护、运行日志四叶板），并用纯受控 React Confirm 美化浮窗取代 native window.confirm 以穿透 Iframe 拦截的开发工作中，不影响 AI 模型对话或配餐提示词设计（保持 `src/prompts.ts` 完全向下兼容）。

### 🚀 2026-06-29 - [V2.6.0] 原料购销及仓储库存台账系统功能发布
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代设计并上线了全新的原料购销台账及库存仓储系统，涉及四个默认台账的增删改查、每日出入库录入、实时物理库存自动核算以及入库单、出库单的生成与 CSV/PDF 导出。本项改动完全属于仓库库存物理台账及单据打印功能模块，未改动位于 `src/prompts.ts` 内的 AI 营养配餐审计系统角色及提示词，AI 模块 100% 保持无缝绿灯兼容。

### 🚀 2026-06-29 - [V2.7.0] 原料购销台账双样式切换与出入库单规范化升级
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次新增「样式一：食品原材料购销总表」与「样式二：单原料日出入库月流水账」两种台账视图模式，并全面规范了出入库单据 of 字段（新增类别、序号、品名、出库人、接收人等），同时实现了批量一键应用签字功能。所有改动均属于台账数据录入与凭证管理范围，未修改 `src/prompts.ts` 的任何 AI 提示词内容，AI 模块保持 100% 无缝绿灯兼容。

### 🚀 2026-06-30 - [V2.8.0] 全自动数据双模持久化同步与部署文档
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代开发了后端数据持久化服务 `StorageService` 与前端防抖数据同步助手 `SyncHelper`，实现本地单机 JSON 文件落盘与腾讯云 COS 对象存储部署的平滑切换，并且在 `readme_zh.md` 中补充了详细的部署文档。此项改动完全属于系统持久化层与部署运维手册补充，未对 `src/prompts.ts` 中的 AI 审计诊断角色及结构化提示词模板进行任何修改，保持 100% 向下兼容。

### 🚀 2026-06-30 - [V2.9.0] 台账餐位分组 1:1 对齐、单向数据同步、查看历史账期与原料库下拉优化
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代实现了台账系统与备餐餐位分组的一一对应与级联同步，当手动录入台账入库数据时，可按年月日自动单向同步到备餐记录；支持备餐报表按年月日账期进行过滤查看和隔离保存；在编辑原料（新增台账原料、修改原料、手动新增备餐项）时引入了基于全局大底库的 `SearchableSelect` 可搜索下拉框，避免输入不存在的原料；同时在管理后台新增了原料大底库的 CRUD 字典维护。此项改动均为本地业务流与 UI 输入检索优化，未改动位于 `src/prompts.ts` 内的 AI 营养配餐审计系统角色及提示词，AI 模块 100% 保持向下兼容。

### 🚀 2026-06-30 - [V2.9.1] 台账录入暂存、批量保存与退出自愈机制
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代为台账录入功能引入了确认录入与缓存自愈机制。输入框默认锁定，点击“开始录入今日采购数据”后启用，编辑过程中的所有采购信息均会实时缓存在浏览器 LocalStorage 中。若发生意外断网、刷新或离开页面，缓存不会丢失，再次点击录入即可轻松找回。录入完毕点击“确认”后将批量存盘同步并清理本地草稿缓存。此改动为纯前端数据交互安全升级，未修改 AI 提示词，AI 模块 100% 兼容。

### 🚀 2026-06-30 - [V2.9.2] 自动生成受众Key、外观表情Emoji下拉化及默认受众微调
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代微调了一级受众在初次启动（无物理缓存）时的默认种子项，调整为：幼儿、在校生、教师三个一级餐卡群体。同时，重构了管理后台新增受众模块，唯一识别Key由系统自动生成（不允许手动修改），外观展示Emoji改为内置下拉菜单挑选。此项改进不影响数据契约，AI提示词100%兼容。

### 🚀 2026-06-30 - [V2.9.3] 备餐报表（餐位分组）页面纯只读化与后端数据传输限额调高
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代对餐位分组页面（备餐报表）进行了彻底的纯只读重构：删除了手动“新增食材”、“删除食材”及“批量调价”控制按钮，将报表主网格及日卡片聚焦中的数量、单价输入框全部改造为纯文本展示形态，彻底锁定篡改路径，使其所有采购数据只能来自于台账的单向同步计算。同时，将后端 `/api/storage/save` 所采用 of Express Body Parser 限额调高至 50mb，避免极端大容量数据同步时触发 `PayloadTooLargeError`。此改动为界面安全性控制，AI 模块 100% 兼容。

### 🚀 2026-06-30 - [V2.9.4] 台账物理同步归档修复与前台原料参数修改彻底禁用
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代彻底禁用了台账前台录入页面的“新增原料采购项”、“编辑/删除原料参数”功能，将原料的属性维护统一收归管理后台。同时修复了台账物理同步的两处关键漏洞：1. 在 `ledgerStore.ts` 的 `notifyListeners` 中补齐了 `SyncHelper.triggerSyncToServer()`，确保台账任何改动都物理同步写入 `data/db.json`，解决刷新页面数据回滚为0的问题；2. 在 `store.ts` 的 `syncFromLedger` 中将本地存盘 `saveToStorage` 升级为 `saveConfigAndNotify`，确保台账与备餐报表进行联动同步广播与自动服务器存盘。此改动为底层同步与页面安全加固，AI提示词100%兼容。

### 🚀 2026-06-30 - [V2.9.5] 级联同步原料字典与彻底清除备餐单日批量调价
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代彻底删除了餐位分组（备餐管理）单日聚焦卡片（FOCUS 视图）中最后一处遗留的“批量修改同列价格”的按钮与回调接口；同时打通了管理后台“原料字典管理”与前台首页的级联更新机制：当管理员在后台修改某原料的名、类别、单位或进行删除时，系统自动在 `ledgerStore.ts` 和 `store.ts` 中执行级联同步（`cascadeUpdateMaterial`/`cascadeDeleteMaterial`），更新已存台账记录与备餐明细中的对应属性，并通过 `SyncHelper.triggerSyncToServer()` 自动保存到后端 `data/db.json`，彻底保障了原料属性的单点维护与全网生效。

### 🚀 2026-06-30 - [V2.9.6] 管理后台原料参数全面动态绑定展示与新增备注(规格)字段
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代打通了原料字典与前台首页展示的动态绑定逻辑：1. 在 [rawMaterialDict.ts](file:///Users/sangsang/Desktop/KPMSS/src/rawMaterialDict.ts) 的 `RawMaterialDictItem` 中引入了可选的 `remark` (备注) 字段，并在默认种子数据中预设了规格备注；2. 在 [AdminBackend.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/AdminBackend.tsx) 中增设了备注(规格)输入框，且让管理员的添加/修改都可以录入这一属性；3. 重构了台账主表格、出入库凭证单及备餐网格，其食材名称、计量单位和规格备注全部动态解析自最新的原料字典。这意味着在管理后台修改原料参数后，首页的全部旧条目与对应内容将 100% 动态刷新。此项改动不影响任何提示词及 AI 计算架构，保持完全兼容。

### 🚀 2026-06-30 - [V2.9.8] Windows 7 老旧客户端显示性能优化与一键部署说明支持
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代对系统针对老旧操作系统（如 Windows 7）及旧版浏览器进行了专项兼容性加固：1. 在 [index.html](file:///Users/sangsang/Desktop/KPMSS/index.html) 中引入 Noto Sans SC 谷歌字体包，并将“Microsoft YaHei”微软雅黑的优先级提至首位，配合 ClearType 解决 Win7 默认宋体锯齿丑陋问题；2. 在 [index.html](file:///Users/sangsang/Desktop/KPMSS/index.html) 中引入 Twemoji 彩色表情全局转换脚本与 MutationObserver 动态更新解析器，在旧版系统不支持彩色 Emoji 时自动以 SVG 矢量图替代，解决 Emoji 显示为黑白方块的异常；3. 优化全局滚动条与台账大表格的最小宽度自适应限制，防止老旧显示器下容器遮挡显示不全的问题；4. 同步更新中英文 [readme_zh.md](file:///Users/sangsang/Desktop/KPMSS/readme_zh.md) 追记 Windows 7 主机的 Node 14.15 兼容版本运行、打包和调试文档。

### 🚀 2026-06-30 - [V2.9.9] 餐位受众侧边栏折叠支持与备餐子模块独立按需显隐工具栏
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代完成了餐位受众页面功能布局的模块化重整：1. 在 [App.tsx](file:///Users/sangsang/Desktop/KPMSS/src/App.tsx) 的左侧Aside导航中引入了 `isSidebarCollapsed` 状态，并在侧边栏顶条添加了折叠按钮“◀/▶”，允许用户将“活动餐位分组”列表手动横向折叠为只显示图标Emoji的紧凑状态，最大化记账表展示面积；2. 隐藏了原受众主面板顶部默认直接展示的“日开支走势、各品类占比、重点监控”以及底部的“AI食堂规划”模块；3. 在 [App.tsx](file:///Users/sangsang/Desktop/KPMSS/src/App.tsx) 中新增了辅助功能切换控制栏与 `activeSubTools` 状态变量，默认只展示“每日采购细表”（即 `TableGrid` 组件），用户可通过点击按钮动态开启或再次收起“日开支走势”、“品类资金占比”、“重点监控 Top5”及“AI膳食配餐分析”等各大诊断版块，全面提升了系统的可定制性与界面的清爽度。

### 🚀 2026-06-30 - [V3.0.0] 备餐与台账采购细表新增字典下拉选择添加与悬浮删除原料行
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代重构并补全了备餐管理与购销台账中的原料采购行“添加与删除”操作，且强制不要添加字典不存在的原料以防脏数据：
  1. **备餐（每日采购细表）**：在 [TableGrid.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/TableGrid.tsx) 顶部的搜索栏右侧新增了“添加原料”下拉选择器（使用 `dictOptions` 获取字典里本品类的所有原料），只允许下拉挑选并绑定对应单位一键添加；在大宽矩阵行首以及聚焦日卡片的右上角加装了悬浮垃圾桶删除按钮，实现原料行删除功能。
  2. **原料购销台账**：在 [LedgerSystem.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerSystem.tsx) 顶部控制栏的未录入态下直接嵌入“添加原料采购项”下拉框，可直接下拉挑选原料并级联获取字典单位和规格一键新增至采购清单；在大宽总表模式的第一列加装了悬浮 Trash2 垃圾桶按钮，只在未开启录入模式时启用删除，保障了录入与维护状态安全。

### 🚀 2026-06-30 - [V3.1.0] 管理后台原料参数换算单位/比例扩展及台账自动联动折合
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代打通了原料的“多单位换算与录入关联折合”功能：
  1. **数据合约扩展**：在 [rawMaterialDict.ts](file:///Users/sangsang/Desktop/KPMSS/src/rawMaterialDict.ts) 的 `RawMaterialDictItem` 接口与 [ledgerTypes.ts](file:///Users/sangsang/Desktop/KPMSS/src/ledgerTypes.ts) 的 `DailyStockRecord` 接口中，扩展了 `conversionUnit` (换算单位)、`conversionRatio` (换算比例) 和 `conversionUnitQuantity` (折合后的换算单位数量值) 属性，并在系统默认推荐种子数据（如大米、面粉、大豆油等）中预设了袋折合斤（50斤）的配置。
  2. **管理后台（AdminBackend.tsx）**：在“核心原料库字典管理”的新增/修改表单中，添加了“换算单位”和“换算比例”两个可选属性字段，支持管理员设定任意原料的多级单位折算，并在原料卡片列表中直观展示 “换算: 50斤/袋” 等折合说明，且支持编辑保存时的数据重置。
  3. **台账首页联动（LedgerSystem.tsx）**：在用户录入台账（Style 1 购销总表）的采购数量（inQuantity）时，如果对应的字典原料中定义了换算规则，系统在 `handleDraftCellChange` 时自动计算 `conversionUnitQuantity = 袋数 * 50` 并自动保存入库；同时在采购数量输入框下方，即时渲染折合后的标签说明（例如输入2袋时，下方动态显示“折合: 100.0 斤”），极大地提升了财务核对效率与用户体验。

### 🚀 2026-06-30 - [V3.2.0] 全局系统字体等比放大、按钮明晰度提升及细表横竖高对比度优化
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代针对前台大屏与日常视认易用性，执行了系统级别的UI视觉强化：
  1. **全局字体与按钮清晰化**：在 [index.html](file:///Users/sangsang/Desktop/KPMSS/index.html) 中将 HTML 根字号设为 `16px` 并整体等比例扩展，提升了界面阅读视认性；为所有按钮、输入框、下拉选单补强了更明显的阴影变换与描边效果，使其点击边界更立体、更明晰；将滚动条色彩整体加深。
  2. **大宽总表矩阵高对比强化**：在 [TableGrid.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/TableGrid.tsx) 中重塑了采购细表格：
     - 将每日采购大宽矩阵的正文字体全部升级（从 `10px`/`11px` 提升到 `xs` 字号），并在首列食材名称列中应用粗黑加重字体渲染。
     - 大幅加强了表格所有内部的网格边框线颜色（由 `gray-100` 升级为 `slate-200` 和更深色），横竖分隔线清晰凌厉。
     - 为总累加汇总列及表底每日开支合计行（`bg-slate-200`）使用了高反差底色及加重双像素边界线，彻底告别原有的模糊朦胧感。

### 🚀 2026-06-30 - [V3.3.0] 台账入库与备餐细表双向深度融合与明细一一对应
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代彻底解决了台账与备餐细表“数据不同步、游离和不对齐”的痛点，确保其“一一对应”且“无记录不显示”：
  1. **台账服务层（ledgerStore.ts）**：在 `LedgerService` 内部暴露出静态读取接口 `getLedgerItems()` 获取全量实时采购明细，并新增静态方法 `updateDailyRecordByKey()` 支持通过 `ledgerId + 原料名 + 日期` 直接写入台账出入库属性。
  2. **细表直接对齐台账（TableGrid.tsx）**：重构了采购细表的过滤拦截器 `filteredItems` 的计算逻辑，其渲染的每日备餐数据（数量、单价、金额）将直接映射并绑定对应日期下该客群（`targetGroup`）对应的台账原料采购记录（`inQuantity`/`inPrice`），从而使得若台账当天无录入明细，则采购细表中对应的一天也将完全显示为0（不显示对应值），保证 100% 数据一致。
  3. **单元格双向联动（store.ts）**：重构了备餐报表修改单元格的静态方法 `updateCell`，当用户在采购细表中修改某行某天的用量/单价时，系统除了自我留存，还会通过 `updateDailyRecordByKey` 反向自动同步改写原料台账里对应项目的当日入库指标，自动对齐折合比例并保存，达成了无缝的双向同步机制。

### 🚀 2026-06-30 - [V3.4.0] 餐位人群删除级联删除对应台账与新建餐位空细表热加载
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代对一级受众人群及台账的生命周期联动进行了闭环升级，符合安全干净的初始化规范：
  1. **级联删除台账**：在管理后台 [AdminBackend.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/AdminBackend.tsx) 删除某一级人群时，调用 `deleteGroup` 会级联调用台账服务的 `syncDeleteLedgerFromGroup`，物理级联删除对应标识 ID 的台账及其下所有原料，确保不留僵尸数据。
  2. **新建人群空原料限制**：重构了 [ledgerStore.ts](file:///Users/sangsang/Desktop/KPMSS/src/ledgerStore.ts) 中的 `syncLedgerFromGroup`。当在后台新建餐位人群时，生成的对应台账下的初始原料项目集合（items）直接初始化为空数组 `[]`（不再自动克隆加载常量种子原料），从源头上规避了默认脏数据。
  3. **细表直接源自台账渲染（TableGrid.tsx）**：将 `filteredItems` 的核心过滤基础从 `report.items` 彻底重构为以台账内的采购原料项目为基准。这就意味着新建客群时因为台账原料为空，每日采购细表默认干净呈现，不显示任何原料。当用户在台账里安全添加了原料并在对应采购项中录入保存之后，每日采购细表就会自动触发热更新，跟随台账存在数据 and 实际原料来进行渲染与开支汇总，极高地增强了数据可读性。

### 🚀 2026-06-30 - [V3.5.0] 全量真实食堂食材与备品原料底库预载配置
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代依据用户提供的真实食堂备货底库，对系统首次启动 of 初始数据种子进行了全量替换：
  1. **原料大字典重载（rawMaterialDict.ts）**：重写了 `RawMaterialsDictService.generateDefaultSeeds()` 方法。引入了包括 20 种蔬菜类（如酵母、豆沙、白菜、木耳、干豆腐等）、9 种粮油类（如大米、豆油、挂面等）、10 种调料类（如盐、酱油、十三香等）、13 种肉类（如精五花、羊肉片、鸡蛋等）、3 种低耗品（如一次性手套、大黑袋等）和 6 种水果类（如沃柑、香梨、沙白瓜等）共 61 种真实食材原料。
  2. **精确单位换算预设**：为部分需要批量折算的大包装食品内置了精准的比例和单位换算参数（例如：大米和面粉默认为 25kg/袋且折合比例为 50斤；鸡蛋默认为 360枚/箱且折合比例为 360枚；沃柑和苹果默认为 10kg/箱且折算为 20斤等），极大地省去了财务后续手工计算的麻烦。
  3. **管理后台实时操作**：上述 61 种新食材已被系统永久保存至底层存储空间。用户可直接在管理后台的“原料库字典管理”板块对其进行实时查看、修改计量单位和修改备注比例，首次载入零等待。

### 🚀 2026-06-30 - [V3.6.0] 管理后台原料字典支持品名模糊检索与大类条件查找筛选
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为了解决拥有 61 项原料的大底库不易肉眼快速定位的问题，本次迭代增设了搜索和查找过滤器：
  1. **新增查找条件状态**：在 [AdminBackend.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/AdminBackend.tsx) 的“核心原料库字典管理”板块上方，新设计并注入了一个检索查找控制栏。引入了 `dictSearchQuery` (原料品名模糊匹配) 和 `dictSearchCategory` (所属二级食材分类) 两个内部状态变量。
  2. **卡片列表动态过滤**：对原料字典列表的 map 循环添加了前置 filter 拦截：
     - **品名模糊匹配**：用户可在输入框内输入原料拼音或中文（如“大”或“黑”），系统会自动模糊匹配展示含有该关键字的所有原料；
     - **分类快速筛选**：用户可通过下拉菜单直接切换到指定的二级品类大类（如“粮油”、“蔬菜”或“肉类”），实现垂直品类一键查找筛选。
  3. **检索快速重置**：提供了一键“重置搜索”的便捷按钮，便于管理员快速清空查找条件，返回无损的全量底库卡片网格中。

### 🚀 2026-06-30 - [V3.7.0] 全系统原料字典、采购细表及台账明细支持拼音首字母与全拼模糊查找
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次迭代通过引入轻量级拼音转换模块，实现了对三大核心查询场景下输入框查找功能的拼音及首字母（Pinyin）智能化扩展：
  1. **安装拼音工具依赖**：引入并配置了免维护轻量 JS 工具包 `pinyin-pro`，确保在 Windows 7 离线部署环境中也能快速提取汉字拼音。
  2. **编写全局拼音匹配工具（utils.ts）**：在 [utils.ts](file:///Users/sangsang/Desktop/KPMSS/src/utils.ts) 中声明并导出了全局方法 `matchPinyin(targetText, querySearch)`：
     - 自动提取汉字的全拼并去除空格（如“大豆腐” 映射为 “dadoufu”）；
     - 自动提取首字母缩写拼音（如“大豆腐” 映射为 “ddf”）；
     - 判断用户输入的搜索字符是否为上述全拼、缩写或汉字原文的任意子串，实现不分大小写的秒级模糊匹配。
  3. **三大界面全面升级拼音匹配**：
     - **后台原料字典页**：修改 [AdminBackend.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/AdminBackend.tsx) 中的卡片列表过滤器，调用 `matchPinyin` 进行模糊过滤。
     - **每日采购细表页**：修改 [TableGrid.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/TableGrid.tsx) 表格的过滤依赖，将 `searchQuery` 的文本过滤更换为拼音及首拼查找，如输入 `ys` 可精确筛查出 `圆葱/油菜` 等。
     - **购销台账系统主表**：修改 [LedgerSystem.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerSystem.tsx) 表格中的过滤规则，使得台账主表按名称查找原料时同样完美享受拼音拼读首拼匹配体验。

### 🚀 2026-07-01 - [V3.8.0] 部署说明书扩写：增补多端口切换、对象存储开关与 Nginx 域名共存隔离说明
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为符合云环境多应用并存部署、防止端口冲突及确保高数据安全性的生产实践，对 [readme_zh.md](file:///Users/sangsang/Desktop/KPMSS/readme_zh.md) 中“云端部署指南”章节进行了详尽扩写：
  1. **端口定制配置**：增补了利用 `.env` 中的 `PORT=XXXX` 变量或者命令行注入形式手动选择和改写监听端口的详细说明。
  2. **云存储冷备份按需切换**：理清了使用 `STORAGE_TYPE` 控制选用本地单机存储（`local`）或云对象存储（`cos`）的详细步骤。
  3. **子域名与 Nginx 反代理路由**：提供了专属 Nginx Server Block 虚拟主机配置模板。详细讲解了如何通过域名绑定进行精确的流量转发，并说明了如何通过此方法实现与服务器上其它正在运行的 Web 服务共存隔离、互不影响。

### 🚀 2026-07-01 - [V3.9.0] 剔除洗洁精、清空首次启动台账数据与前台原料字典存在性强校验
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：依据食品账单规范和数据干净初始化要求，对台账底册初始化与展示校验规则进行了深度加固：
  1. **移出非食品耗材**：在台账初始推荐列表 `PRESET_LEDGER_MATERIALS` (即 [ledgerConstants.ts](file:///Users/sangsang/Desktop/KPMSS/src/ledgerConstants.ts)) 中永久移出了“洗洁精”这一非食品低耗品，并将“食用盐”校正为“盐”，确保食品账目的纯粹。
  2. **清空首次启动台账初始原料**：重构了 [ledgerStore.ts](file:///Users/sangsang/Desktop/KPMSS/src/ledgerStore.ts) 中的首发缓存丢失生成函数 `generateSeeds()`。首次启动时，仅预生成“幼儿备餐”、“在校生备餐”、“教师备餐”三个空台账，其下的 `ledgerItems` 原料列表强制设为 `[]` 空数组，实现完全的“空台账初始化”。
  3. **前台大底库存在性防脏强校验**：
     - **购销台账系统**：在 [LedgerSystem.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerSystem.tsx) 的 `currentLedgerItems` 和 `filteredLedgerItems` 计算属性中增加查表校验，要求展现和参与出入库录入的原料名称必须存在于最新的原料字典服务 `RawMaterialsDictService.getItems()` 中，否则不予展现；
     - **备餐细表系统**：在 [TableGrid.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/TableGrid.tsx) 每日采购细表的 `filteredItems` 渲染钩子中加装了相同的 `dictItem` 校验。一旦检测到台账中某采购条目对应的原料名称被管理员从后台字典中删除注销，其便会自动在细表中物理消失，彻底避免了后台不存在的品类出现的问题。

### 🚀 2026-07-01 - [V4.0.0] 台账添加原料采购项下拉选择组件支持拼音全拼及首字母模糊查找
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为了让库管在台账数据录入过程中能够极其高效地添加原料采购项，对搜索下拉选择器组件进行了拼音检索赋能：
  1. **改造下拉选择器（SearchableSelect.tsx）**：在 [SearchableSelect.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/SearchableSelect.tsx) 中引入了全局拼音模糊查找助手 `matchPinyin`。
  2. **支持拼音和汉字模糊匹配**：将下拉菜单内的 `options.filter` 条件由原先 the `includes` 简单字符串包含校验，升级为使用 `matchPinyin` 进行多端比对。现在，用户在下拉框内不仅可以输入汉字（如“排骨”），也可以手动输入拼音全拼（如 `paigu`）或拼音首字母（如 `pg`），下拉选项便会同步秒级高精度收窄过滤，极大地提升了前台录入人员的记账添加体验。

### 🚀 2026-07-01 - [V4.1.0] 台账添加原料采购项前置模糊联想查找输入框
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为使台账录入状态下无需展开下拉列表即可检索匹配，对添加原料表单的操作逻辑进行了流线化加固：
  1. **引入检索 State（LedgerSystem.tsx）**：在台账系统 [LedgerSystem.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerSystem.tsx) 中声明了内部状态 `addMaterialSearchQuery` (添加原料检索词)。
  2. **前置模糊联想输入框**：在台账的“添加原料采购项”表单中，于选择下拉选单前方，直接注入了一个极小化的高对比文本输入框。当操作人员在此键入中文字符、拼音首拼（如 `dm`）或拼音全拼（如 `dami`）时，系统会自动在下拉选择列表（Select Options）中调用 `matchPinyin` 进行级联模糊筛选。
  3. **选项联想过滤**：下拉列表会智能隐藏所有不包含该拼音/中文特征的冗余项。录入员输入关键字筛选后即可点击右侧的选择框一秒选定匹配项（如搜索 `bls` 仅保留 `巴沙鱼` 选项），点击“添加”即可即时录入台账，体验极其丝滑。

### 🚀 2026-07-01 - [V4.2.0] 台账新增原料栏上线拼音/汉字绝对定位悬浮联想面板
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：应录入员在极速录入时直接通过悬浮浮窗点击定位的需求，彻底重构了原料快捷添加卡片的交互模型：
  1. **摒弃原生 Dropdown**：删除了原本不便操作、在旧款 Win7 上容易卡顿失真的原生 HTML `<select>` 标签。
  2. **悬浮联想浮窗（absolute dropdown）**：在输入框正下方设计了一个 `absolute left-0 right-0 z-50` 绝对定位挂载的检索结果容器浮标面板。
  3. **随输随显，实时渲染**：当录入员点击聚焦或输入拼音（全拼/声母如 `ym`/`yanmai`）或汉字时，浮窗会自动弹出并同步渲染出高亮匹配的字典原料项（展示名称、单位、品类分类等参数）。
  4. **点击选中，一键添加**：看到了想要的原料，直接点击该悬浮行，便会智能填充输入框、选定原料参数并安全关闭浮窗，接着点击右侧的“添加”按钮即可瞬间完成添加，极大地契合了高频流水录入员的盲打与快捷交互习惯。

### 🚀 2026-07-01 - [V4.3.0] 采购细表大矩阵三子列（数量/单价/金额）加粗纵向边框与背景色强对比优化
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：针对部分低分屏或视力疲劳的用户，在大宽总矩阵报表中因数量、单价和金额紧挨着而产生的“在同一个单元格里”的视觉错觉，进行了精细的外观对比度重绘：
  1. **二级列头加装分隔边框**：在 [TableGrid.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/TableGrid.tsx) 二级表头的“数量”、“单价”和“金额”中加粗竖分隔边框，由原先的透明无边框调整为 `border-r border-slate-200` 及 `border-r border-slate-300` 强对比线。
  2. **数据单元格纵向切分**：对每天大日历下的三个数据单元格（`<td>`）加装了清晰的纵向线。将原本同背景的渲染方式重构为：
     - **数量子格**：配置 `border-r border-slate-200 bg-white` (经典纯白)；
     - **单价子格**：配置 `border-r border-slate-200 bg-slate-50/40` (微灰质感)；
     - **金额子格**：配置 `border-r border-slate-300 bg-sky-50/40` (微蓝高亮)。
  3. 通过这一对比色和物理分隔线的介入，用户在横向滑动大表格时，能瞬间区分开数量、价格和金额的数值列，表格立体感和视认性显著增强。

### 🚀 2026-07-01 - [V4.4.0] 台账今日数据录入模式激活状态下支持随时添加原料采购项
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：去除了原先台账录入状态下无法操作原料结构的阻塞性规则，以提供无感极速交互：
  1. **移除录入模式表单屏蔽**：在 [LedgerSystem.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerSystem.tsx) 中移除了原本包裹在添加表单外层的 `{!isRecordingMode && ( ... )}` 限定。
  2. **支持数据录入中随时加塞项目**：现在，即使操作员已经点击了“`开始录入今日采购数据`”并且输入框处于解锁可编辑、正填写数字的阶段，顶部的新增联想匹配栏依然保持常开、完全可用。
  3. **数据实时对齐与自愈**：当录入员发现今日新送来某样从未记账过的原料时，无需中途保存并退出当前录入草稿，直接在顶部搜索、点击选中并添加该原料，台账列表中会立即热插拔生成该新原料行。新增行的当日输入框同样自适应解锁可编辑状态，让库管在单次操作流程中顺畅完结全部记账，极大地提升了现场备餐记账的容错率。

### 🚀 2026-07-01 - [V4.5.0] 台账总表二级分类勾选及单原料流水纯净网页打印功能装配
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：依照食堂食品卫生检查台账纸质签字存档的硬性规范，在台账控制台全量上线了双重纯净打印功能：
  1. **图一总表打印（登记总表模式）**：
     - **二级分类筛选弹窗**：在总表模式（`style1`）的右上角新增“`打印登记总表`”按钮。点击后会弹出美观的二级分类多选勾选栏，支持一键全选/全消。
     - **总表打印预览**：允许操作员自定义勾选要打印的食材分类（蔬菜、粮油、调料、肉类、低耗品、水果），确认后即可生成排版规整、隐藏屏幕操作按键的“食品原料收支购销登记总表”打印预览。预览表支持当日入库数、单价、入库金额、今日出库数、当月截止今日的累计结存库存、采购验收保管人签名、供货商和备注的纵向完整输出，自动计算总金额大小写。
  2. **图二单原料月度流水打印（单原料日流水模式）**：
     - **月度卡片打印入口**：在图二样式的右上角新增“`打印月度流水`”按钮。
     - **卡片式流水打印预览**：点击后直接生成针对该单一原料本月 1 号至 31 号的“单一原料出入库月度流水卡片”纯净大报表。报表头部明晰显示主要供货商与索证状态，表身自动累算呈现每日采购、经办人、生产日期、保质期、感官性状、验收检测、领用出库、每日结存与保管员签字的清爽网格。
  3. 打印时自动隐藏所有浏览器网页元素（如返回按钮和提示词），调用 `window.print()` 可直接生成完美的纸质版。

### 🚀 2026-07-01 - [V4.6.0] 原料库存总览面板上线（菜单栏快速入口 + 多维筛选 + 预警提示）
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：全量上线独立的库存总览监控面板，让库管员随时随地掌握仓储动态：
  1. **顶部菜单栏快速入口**：在系统顶栏（任意页面均可访问）的"管理后台"旁新增了"📦 库存总览"按钮，点击即弹出独立全屏级模态面板，无须切换页面。
  2. **新建独立组件 `InventoryPanel.tsx`**：
     - 实时订阅 `LedgerService` 数据，确保每次打开均展示最新库存快照。
     - **防脏校验**：仅展示在后台原料字典 (`RawMaterialsDictService`) 中存在的原料，自动过滤已被删除的脏数据。
  3. **多维度筛选功能**：
     - **台账筛选**：可按台账（如"幼儿备餐"、"教师备餐"等）过滤，仅看该台账原料。
     - **二级分类筛选**：可按蔬菜、粮油、调料、肉类、低耗品、水果等分类单独查看。
     - **拼音/汉字模糊搜索**：支持全拼、声母首拼和汉字的综合模糊匹配，快速定位原料。
     - **低库存预警快捷切换**：一键过滤仅显示当前库存 ≤5 的预警品种，方便紧急补货决策。
  4. **库存状态三色预警系统**：
     - 🟢 **库存充足**（> 5）：正常行底色 + 绿色字体显示。
     - 🟡 **库存偏低**（≤ 5）：琥珀色背景高亮行 + 橙色"偏低"标签 + 橙色字体。
     - 🔴 **极低/归零**（≤ 1）：玫红背景高亮行 + 红色"极低"标签 + 红色加粗字体。
  5. **可排序表格**：支持按"原料名称"、"历史入库累计"、"历史出库累计"、"当前库存"四个字段升序/降序切换排序，点击表头即可。
  6. **顶部概览统计卡片**：面板头部展示当前筛选视图下的"总品种数"、"库存偏低数"、"极低/归零数"三项核心汇总指标，一眼掌握全局风险。
  7. **展示列**：原料名称 + 状态标签 / 所属分类 / 台账归属 / 规格 / 单位 / 历史入库累计 / 历史出库累计 / **当前实时库存**。

### 🚀 2026-07-01 - [V4.7.0] 台账总表打印样式按图示重构（购销台账官方表头格式）
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：完全重构了 [LedgerSystem.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerSystem.tsx) 中样式一（`printPreviewStyle === "style1"`）的打印排版，具体变更如下：
  1. **大标题调整**：由原来的"台账名称 —— 食品原料收支购销登记总表"改为"**{台账名称}食堂食品原材料购销台账**"，居中展示，带全宽上下边框，无副标题信息行。
  2. **表格结构全面重构**（从原 13 列调整为图示 11 列）：
     - 移除：序号、分类、单价、入库总额、当日结存、备注/去处列
     - 新增：食品索证、感官性状、供货商及地址、采购时间列
     - **出入库时间**改为跨 2 列的合并表头（第一行显示"出入库时间"，第二行拆分为"入库"和"出库"两个子列）
  3. **列宽比例**：使用 `tableLayout: fixed` + `<colgroup>` 精确控制各列宽度，保持图示比例。
  4. **数据填充逻辑**：数量 = 入库数量；采购时间 = 入库时显示选定日期；出入库时间入库列 = 有入库时显示日期；出库列 = 有出库时显示日期；未有记录的格子留空（不显示"-"）。
  5. **自动补空行**：当原料数据不足 15 行时，自动补充空白行，确保打印页面整洁不空旷，方便手工填写。
  6. **底部签字栏简化**：改为左右两端对齐的"主管审核"和"打印日期"。

### 🚀 2026-07-01 - [V4.8.0] 台账录入模式支持手动修改采购/入库时间和出库时间
- **提示词变更状态**：**无变动 (No Changes)**。
- **用户需求**：录入今日采购数据时，采购时间和出入库时间默认是今天，但要允许用户手动修改。
- **说明**：在台账数据录入系统中全面扩展了时间编辑能力，具体变更如下：
  1. **新增 `outDate` 字段**：在 `DailyStockRecord` 接口中新增 `outDate?: string` 字段，用于独立存储出库时间（与 `purchaseDate` 入库/采购时间分离管理）。
  2. **录入表格新增两列**：
     - **采购/入库时间列**（绿色底色）：`type="date"` 日期输入框，默认显示当天日期，用户可手动选择修改，写入 `purchaseDate` 字段。
     - **出库时间列**（蓝紫色底色）：`type="date"` 日期输入框，默认显示当天日期，用户可手动选择修改，写入 `outDate` 字段。
     - 两列均在非录入模式下禁用，视觉呈灰色。
  3. **表头同步更新**：在供货商列与采购员列之间，插入"采购/入库时间"和"出库时间"两个新列表头。
  4. **打印模板时间读取优先级升级**：
     - 采购时间列：`record.purchaseDate || selectedDate`（优先使用用户手动设置的值）
     - 出入库时间-入库列：`record.purchaseDate || selectedDate`
     - 出入库时间-出库列：`record.outDate || selectedDate`（使用独立的出库日期字段）

### 🚀 2026-07-01 - [V4.9.0] 单原料日流水打印样式重构
- **提示词变更状态**：**无变动 (No Changes)**。
- **用户需求**：将单原料日流水卡片（图二）页面下的打印样式调整为指定图片格式。
- **说明**：完全重构了 [LedgerSystem.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerSystem.tsx) 中样式二（`printPreviewStyle === "style2"`）的打印渲染排版：
  1. **标题和日期居中**：主标题为“宾县第二小学食堂食品原材料购销台账”，并在第二行增加居中的“日期：（        ）”手工填空项。
  2. **混合合并嵌套表头**：
     - 表头第一行包含基础信息：“采购项目”（原料名）、“经销商”（主要供货商）、“索证索票”（索证说明）。
     - 表头第二行采用大分类合并单元格，拆分为“入库（跨7列）”与“出库（跨3列）”。
     - 表头第三行包含具体细分列头：日期、采购数量、采购员、生产日期、保质期、感官性状、检验员、出库数量、当日库存、保管员。
  3. **数据展现与空白行补充**：
     - 只显示有实际出入库变动日期的明细数据行（日期列精简仅显示号数）。
     - 数据不足 15 行时自动补足空行，边框加粗为黑色实线以符合纸质手工台账要求。

### 🚀 2026-07-01 - [V4.10.0] 备餐报表系统彻底重构，仅以台账作为唯一的核心物理数据库
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为了响应数据高纯净度和杜绝冗余的规范要求，我们彻底拆解了备餐报表和台账系统的数据重复存储机制：
  1. **废弃备餐报表中的 `dailyData` 明细存储**：在 `PrepReportService.saveToStorage` 物理落盘时，自动对报表对象执行数据清洗（剥离每个原料条目的 `dailyData` 实体），不再向本地或服务端保存多余的日度数量、单价及金额。
  2. **台账作为唯一核心物理数据库**：所有的出入库、采购数据只在 `KITCHEN_LEDGER_ITEMS_V2` 中独立且安全地进行保存。
  3. **实时反查映射机制**：升级备餐表格（`TableGrid.tsx`），其渲染及统计计算所需的 1-31 号明细均通过实时读取 `LedgerService.getLedgerItems()` 反向重定向映射，实现了完美的单数据源闭环架构。

### 🚀 2026-07-01 - [V4.11.0] 重构拆分 LedgerSystem.tsx 大组件
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为了解决主台账面板组件行数过长（2500+行）引起的维护困难问题，我们对组件进行了物理拆分：
  1. **新建总表打印子组件 `LedgerPrintStyle1.tsx`**：将复杂的官方总表打印预览模板完整移入其中，提高渲染内聚性。
  2. **新建单原料卡片打印子组件 `LedgerPrintStyle2.tsx`**：将多行混合嵌套表头的单原料日流水卡片打印排版模板移入其中。
  3. **精简 `LedgerSystem.tsx`**：主入口中移除了近 300 行打印模板的 HTML/CSS 实现，在顶部引入两个子组件进行条件渲染。这显著降低了主组件的文件复杂度，有利于以后的独立功能单元测试。

### 🚀 2026-07-01 - [V4.12.0] LedgerSystem.tsx 核心大组件二次重构拆分
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为了持续精炼主台账组件的代码行数与结构，我们再次对台账界面进行了二次剥离：
  1. **新建凭证单打印子组件 `LedgerPrintDoc.tsx`**：将原有日出库单、日入库单纯净纸质签字单据的打印预览模板完全抽离，提供独立的 UI 数据映射。
  2. **主程序瘦身**：在 `LedgerSystem.tsx` 中删除了这部分 130 余行的打印代码块，改为按需直接引入并实例化 `<LedgerPrintDoc />`，使主台账大组件物理文件缩减，提升其在 React 差分重绘与维护时的性能与易读性。

### 🚀 2026-07-01 - [V4.13.0] 台账组件与管理后台深度瘦身与拆分（所有组件物理文件完全降至 1000 行内）
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为了彻底实现高内聚低耦合的代码组织方式，并满足单个文件不超 1000 行的维护性指标，我们对核心大组件进行了全方位的物理拆分重构：
  1. **台账系统 (`LedgerSystem.tsx`) 拆分**：
     - 新建 `LedgerStyle1Table.tsx`，独立封装样式一总表及多维度筛选器的表格渲染与过滤状态。
     - 新建 `LedgerStyle2Flow.tsx`，独立封装样式二单原料月度流水网格及供应商、索证大字头输入。
     - 新建 `LedgerInvoiceTab.tsx`，独立封装 Tab 2 的当日出入库归集单据排版。
     - 新建 `LedgerControlBar.tsx`，独立封装包含批量签字、CSV 导出、记账录入开关、以及行内添加联想输入框等在内的控制操纵栏。
     - 新建 `LedgerAddMaterialModal.tsx` 与 `LedgerPrintModal.tsx`，分别剥离新增原料和打印品类多选模态框。
     - 新建 `LedgerPrintPreviewOverlay.tsx`，独立封装打印预览全屏覆盖层。
     - 经过多达 8 个子组件的深度离析后，主文件 `LedgerSystem.tsx` 成功降至 983 行，且编译和运行 100% 保持原有表现。
  2. **管理后台 (`AdminBackend.tsx`) 拆分**：
     - 新建 `AdminDictTab.tsx`，将原料字典库配置的检索卡片、增删改查表单等独立逻辑完全收纳到子组件中。
     - 新建 `AdminGroupsTab.tsx`，将一级人群分类大类的受众列表、表单录入等逻辑抽离到子组件中。
     - 在 `AdminBackend.tsx` 中彻底清除了由于子组件化后遗留的十余个冗余 state 与 crud 交互函数，使文件总行数降至 815 行。
  3. **总结**：系统全部 TSX 组件行数均被稳健控制在 1000 行内，组件权责分明、极易扩展，为未来前后端分离做好了充分的模块化准备。

### 🚀 2026-07-01 - [V5.1.0] 台账录入新增生产日期和保质期选填与样式二自定义时间段打印
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：为了满足食品安全要件的追踪审计要求，我们升级了台账采购录入及单原料流水打印模块：
  1. **台账录入加入生鲜资质要件**：在 [LedgerStyle1Table.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerStyle1Table.tsx)（样式一）大表上新增“生产日期 (type=date)”与“保质期 (type=text)”两个列单元格，在开启录入模式下支持用户自由选填并落盘；在 [LedgerStyle2Flow.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerStyle2Flow.tsx)（样式二）中也将生产日期输入框升级为 date 选项。
  2. **单原料流水支持自定义时间段检索及打印**：
     - 在台账主舱 [LedgerSystem.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerSystem.tsx) 中引入了 `style2StartDate` 与 `style2EndDate` 状态（根据选中日期月份自动首尾自愈，但可自由调整）。
     - 在 [LedgerStyle2Flow.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerStyle2Flow.tsx)（样式二）的流水表格卡片顶部，新设了“采购流水时间段筛选”的日期区段调节器，使得页面仅展示、录入指定时间范围内的明细，并自适应期初结余。
     - 在 [LedgerPrintStyle2.tsx](file:///Users/sangsang/Desktop/KPMSS/src/components/LedgerPrintStyle2.tsx)（图二打印模板）中将日期由单日天号升级为完整的 “YYYY-MM-DD 年月日”，且顶部“日期：（        ）”框自动填充为该自定义开始至结束的完整时间段范围。

### 2026-07-01 - [V5.2.0] 餐位分组页面下每日采购细表设为只读
- **需求**：不允许在餐位分组页面下对每日采购细表里的采购原料数据进行增删改。
- **实现方案**：
  1. 在 [TableGrid.tsx] 中新增 `readOnly?: boolean` 可选属性。当 `readOnly=true` 时：
     - 隐藏顶部工具栏的添加原料下拉表单（新增功能入口）
     - 隐藏 MATRIX 矩阵视图中每行悬浮的垃圾桶删除按钮
     - 隐藏 FOCUS 聚焦卡片视图中每张卡片顶部的垃圾桶删除按钮
  2. 在 [App.tsx] 中调用 TableGrid 时传入 `readOnly={true}`，使餐位分组页面统一进入只读查看模式。

### 2026-07-01 - [V5.3.0] 出库单打印按图片样式重构
- **需求**：台账页面出库单的打印样式按新图片规范重构。
- **实现方案**：
  1. 重写 [LedgerPrintDoc.tsx] 中的出库单模板（PrintOutDoc）：
     - 标题行：出库单（台账名）+ 年份（月日留白供手填），外框为绿色边框；
     - 表格列：类别 | 序号 | 品名（含单位）| 数量 | 出库人 | 接收人 ；
     - 按食品大类做 rowSpan 合并类别列（蔬菜/粮油/调料/肉类/低耗品/水果）；
     - 有数据行后用空行填充至不少于 25 行；
     - 表格外底部展示供货商说明两行；
  2. 入库单模板（PrintInDoc）抽取为独立子组件，保持原有样式不变；
  3. 所有供货商信息硬编码移至 [ledgerConstants.ts] 的 LEDGER_PRINT_OUT_CONFIG 统一管理。

### 2026-07-01 - [V5.4.0] 出库单打印日期自动填充 + 供货商动态提取
- **需求**：出库单标题的年月日由选择的时间自动填入；供货商处填写出库记录中所有品名对应的真实供货商。
- **实现方案**：
  1. [LedgerPrintDoc.tsx] 中解析 selectedDate (YYYY-MM-DD) 自动填充年/月/日到标题行，去除前导零保持自然数显示；
  2. 遍历所有出库条目，优先读取当日记录中的 supplier 字段，如当日无记录则回落扫描历史 dailyRecords 中有供货商的条目；
  3. 按供货商名称去重分组，汇总各供货商所对应的全部品名列表；
  4. 底部供货商区动态渲染格式：供货商：【名称】（品名1、品名2…）；
  5. 若出库记录均未填写供货商信息，则展示灰色占位提示引导用户填写。

### 2026-07-01 - [V5.4.1] 出库单供货商仅读当日记录（修正）
- **修正**：出库单底部供货商区改为仅从当日出库记录的 supplier 字段中读取，不再回落扫描历史记录。

### 2026-07-01 - [V5.5.0] 二级分组每日采购细表按月导出为 CSV 表格
- **需求**：增加功能，将某个餐位分组页面下某个二级分组的每日采购细表按月份导出为表格。
- **实现方案**：
  1. 在 [TableGrid.tsx] 顶部工具栏添加了一个「导出本月细表 (CSV)」按钮。
  2. 实现 `handleExportCsv` 导出方法，复用了系统底层的 `convertItemsToCsv` 转换逻辑，以 UTF-8 BOM 形式输出，支持防乱码直接用 Excel 打开。
  3. 导出的文件名格式为：`[年份]年[月份]月_[餐位分组名]_[食材二级分类名]_采购细表.csv`，内含当月所有天数原料的数量、单价和金额。
  4. 对该按钮及相关导出函数加注了完整的中文注释。

### 2026-07-01 - [V5.6.0] 台账录入新增出库人与接收人字段并在打印中同步
- **需求**：在台账数据录入页面添加出库人和接收人字段，打印出库单时同步填入，若未填则留白。
- **实现方案**：
  1. 在 [LedgerStyle1Table.tsx] (样式一：日清单表格) 底部及表头中，添加了「出库人」和「接收人」的表头和文本输入框，并调整 colSpan 至 18 保证表格对齐；
  2. 在 [LedgerStyle2Flow.tsx] (样式二：单原料流水) 表格尾部，新增了「出库人」与「接收人」对应的表头和输入框，只有当前选中行开启录入模式时可以编辑，否则自动锁定；
  3. 出库人和接收人数据输入时，均无缝同步记录到台账的每日出入库条目的 `outHandler` 和 `outRecipient` 字段中；
  4. 之前完成的出库单打印模块 [LedgerPrintDoc.tsx] 会自动读取当日这两个字段并同步展现到表格对应列中，若没填则保持留白，契合物理单据签字流程。

### 2026-07-01 - [V5.6.1] 修复管理后台白屏与 autocomplete 黄色警告
- **修复**：
  1. 移除了 [AdminBackend.tsx] 里对未定义属性 `onRefreshGroups` 的引用，直接使用组件的自刷新与全局广播机制，解决了进入后台报错 `ReferenceError: onRefreshGroups is not defined` 导致的白屏问题；
  2. 修复 Chrome 等浏览器在口令校验遮罩层针对 password 类型 input 标签报出的 `Input elements should have autocomplete attributes` 黄色提示，在 [App.tsx] 密码输入框中补齐 `autoComplete="new-password"`。

### 2026-07-01 - [V5.7.0] 彻底解耦前端日志查看，新增服务端本地物理日志持久化
- **需求**：增加详细的日志系统，去掉管理后台查看日志的功能，在服务器或本地部署时，把日志保存在本地，包含详细的时间，事件描述。用户通过查看本地文件查看日志。
- **实现方案**：
  1. 在 [server.ts] 中引入了 `LogService` 日志服务类，在本地初始化并创建 `data/app.log` 文件，提供带标准时间戳和等级分类的单行写入能力；
  2. 暴露了 `POST /api/log` 服务端上报路由；
  3. 修改 [utils.ts] 中的 `LogBroker.publish`，在发布日志的同时，触发 Web fetch 异步请求，自动将详细的模块操作、时间与等级信息上传至服务器持久化写入本地 `data/app.log` 中；
  4. 修改了 [AdminBackend.tsx]，移除了“系统调试审计日志” Tab 导航、按钮及内容渲染区，全面砍掉在前端渲染查看日志的模块；
  5. 物理剔除了冗余的 [LogView.tsx] 组件，使项目结构更加精简清晰。

### 2026-07-01 - [V5.8.0] 首屏加载性能跨越式重构（按需懒加载 + 独立分包）
- **原因分析**：
  首次加载慢主要是因为主 JS 文件（超过 1.3 MB）包含了所有的大体量第三方库（Recharts 图表库、pinyin-pro 拼音查找、Markdown 转换组件等）以及暂不需要在首屏展示的管理后台、台账系统和库存自检等大组件代码。
- **解决手段**：
  1. **代码懒加载 (Lazy Load)**：
     在 [App.tsx] 中使用 `lazy` 和 `Suspense` 动态异步加载 `AdminBackend`、`LedgerSystem` 与 `InventoryPanel` 组件。使其只有在用户点击对应按钮时才被按需拉取，首包资源大幅缩减。
  2. **第三方依赖分包策略 (Rollup Split chunks)**：
     在 [vite.config.ts] 配置 `build.rollupOptions.output.manualChunks` 规则，将 React 核心框架、Recharts 图表及 pinyin-pro 拼音库等第三方大类库独立分片打包（生成 `vendor-react`、`vendor-charts` 及 `vendor-pinyin` 独立文件）。
  3. **极致性能效果**：
     首屏加载的主 JS 包体积从 **1,292.87 kB** 直降到 **111.03 kB**（缩减 91.4%），配合浏览器长效缓存，部署在服务器上时的首次进入页面耗时将呈数量级减少。

### 2026-07-01 - [V5.8.1] 修复管理后台添加人群页面的 getGroups 运行时异常
- **修复**：
  1. 修复了 [AdminGroupsTab.tsx] 中调用不存在方法 `PrepReportService.getGroups()` 导致的 TypeError 页面白屏报错；
  2. 全面将其修正替换为正确的 `PrepReportService.getActiveGroups()` 数据服务接口。

### 2026-07-01 - [V5.9.0] 采购细表自适应四色主题重构（支持用户切换与本地记忆）
- **需求**：为餐位分组的页面下的每日采购细表设置主题样式，用户可以自行选择。
- **实现方案**：
  1. 在 [TableGrid.tsx] 中声明了 `ThemeStyle` 接口和 `THEME_MAP` 静态主题映射配置，定义了「天空蓝 (sky)」、「翡翠绿 (emerald)」、「丁香紫 (purple)」、「典雅暗灰 (charcoal)」四套和谐的高画质配色体系；
  2. 在 TableGrid 工具栏上，新增了一个设计美观的主题色块选择盘（小圆形色块），支持用户通过鼠标悬浮点击进行平滑无感的主题外观切换；
  3. 引入 `localStorage.getItem("prep_table_theme")` 实现用户所选主题在浏览器端持久化记忆，刷新或重新进入页面依然保持上次的选择；
  4. 将 MATRIX 日历大矩阵（表头、总计行、单元格金额强调等）和 FOCUS 聚焦卡片（日期选择滑块、卡片高亮边框、日结金额等）的所有配色 class 均解耦为动态的 activeTheme 样式类引用。

### 2026-07-01 - [V5.9.1] 修复管理后台删除一级受众的 deleteGroup 异常
- **修复**：
  1. 修复了 [AdminGroupsTab.tsx] 在保存或删除一级客群时，由于调用了底层不存在的 `PrepReportService.addDynamicGroup/updateDynamicGroup/deleteDynamicGroup` 方法造成的运行时崩溃报错；
  2. 按照数据服务层底层 API，统一重构为对 `PrepReportService.saveGroup` 与 `PrepReportService.deleteGroup` 方法的正确调用；
  3. 编辑状态下，如遇标识 Key 被修改，增加「先删老 Key，后保存新 Key」的安全防幽灵人群保障逻辑。

### 2026-07-01 - [V5.10.0] 废除本地浏览器 LocalStorage 缓存，升级为准实时多端心跳同步架构
- **原因分析**：
  此前系统采用「本地改 LocalStorage -> 防抖全量覆盖云端」设计。在多浏览器多用户高并发使用时，由于各浏览器 LocalStorage 彼此隔离，会导致数据互相全量覆写并导致别人的录入丢失。
- **解决手段**：
  1. **废除 LocalStorage**：从 [store.ts]、[ledgerStore.ts]、[rawMaterialDict.ts] 中物理移除所有 `localStorage.getItem/setItem` 的存取逻辑；
  2. **全局并行预载**：在 [App.tsx] 启动时通过 `Promise.all` 并行加载备餐、台账与原料大字典的初始化接口，防止各模块按需加载时因空内存上传而清空服务器文件；
  3. **一键内存提取器 (MemoryFetcher)**：由 SyncHelper 注册全局提取回调，在防抖同步时动态提取各 Store 最新的内存快照进行打包上传，规避 LocalStorage 媒介；
  4. **心跳校准对齐**：在首屏启动 `10秒静默心跳拉取器`，自动捕获云端服务器数据的变更，并以静默内存赋值方式刷新前台 React 视图，实现多浏览器视窗最终一致性。

### 2026-07-01 - [V5.11.0] 购销与库存台账操作控制台层级化人机交互重构
- **需求**：购销与库存台账界面的界面布局太乱了，按钮之间没有逻辑，调整按钮的层级、位置和顺序，让用户使用更简易方便。
- **重构方案**：
  1. **层级结构化排版**：重构了 [LedgerControlBar.tsx]，采用「双层导视」卡片架构。顶层为大颗粒度 Tab 导航切换与系统运行状态安全气泡，下层为具体业务功能的操作工作台，使整体主次分明；
  2. **【数据录入】工作台模块化**：左侧归拢「新增原料配置」和「选定采购项目」等静态元数据设置，右侧高度聚焦「开启今日录入」（醒目翠绿）➔「保存并同步」（高亮青蓝）/「暂存退出」（板岩灰）的完整录入流程生命周期，将「打印登记表」作为次级辅助按钮，逻辑清晰无干扰；
  3. **【单据归集】精细化色彩隔离**：左侧单独规划带有背景衬底的「签字填充快捷面板」，一键批量应用出库接收人。右侧将「入库管理」与「出库管理」的导出/打印动作独立成两个子卡片板块，分别使用淡青色（入库）与淡蓝色（出库）进行高保真视觉色彩隔离，彻底摆脱了以前灰色按钮堆砌在一行无序混乱的感觉，显著提升了出库打印单据的用户操作易用性。

### 2026-07-01 - [V5.12.0] 新增全局初始化同步安全锁，杜绝引导期间空内存抹杀数据 Bug
- **原因分析**：
  在系统启动及 Promise.all 异步拉取云端数据期间，内存状态（reports, ledgers 等）尚未填充。此时若有其他事件触发了 SyncHelper.triggerSyncToServer，系统会因为 MemoryFetcher 抓到了空内存状态包而防抖上传空数据覆盖服务器，导致台账和备餐突然自动消失。
- **解决手段**：
  1. **初始化安全自锁**：在 [syncHelper.ts] 中引入了 `isInitialized` 状态自锁控制，若安全锁未解开，所有发往云端服务器的 triggerSyncToServer 保存写操作将被强制性拦截；
  2. **就绪解除限制**：在 [App.tsx] 的 Promise.all 数据加载完成 then 回调中，数据安全落盘内存之后才调用 `SyncHelper.setInitialized(true)` 解锁数据写同步，彻底绝后顾之忧。

### 2026-07-01 - [V5.13.0] 前后端闭环式高鲁棒性容错与日志上报系统增强
- **需求**：增强日志系统，如果有报错，一定要捕捉到，打印详细报错警告信息等。注意不要影响性能。
- **实施架构**：
  1. **Node 后端致命异常捕捉**：在 [server.ts] 中注册了 `uncaughtException` 与 `unhandledRejection` 物理进程级监听，并在 Express 路由总线最后挂载了全局错误拦截中间件，将一切 500 崩溃和底层异常的详细堆栈实时安全记录入 `data/app.log`；
  2. **React 前端隔离防白屏屏障**：新建 [ErrorBoundary.tsx] 错误边界组件，一旦子树组件遭遇计算或渲染异常，会显示带有重试按钮和诊断代码折叠的精致降级 UI，阻断了整页白屏的蔓延，并自动调用 LogBroker 上报详细组件堆栈；
  3. **前端全局 JS/Promise 运行监测**：在 [App.tsx] 生命周期中挂载对 window 的 `error` 与 `unhandledrejection` 顶级监听，在零性能损耗的无感状态下，静默拦截并秒级向后端持久化发送客户端发生的异步失败、AJAX崩溃或脚本报错；
  4. **核心业务舱覆盖**：在 [App.tsx] 将 `AdminBackend` (行政后台)、`LedgerSystem` (台账中心)、`InventoryPanel` (库存面板) 全面包裹于 ErrorBoundary 中，形成坚不可摧的安全沙箱。

### 2026-07-01 - [V5.14.0] 台账录入字段输入框默认宽度充实及字符自适应撑宽优化
- **需求**：台账录入时，记录里面的字段的输入框太窄了，要默认够宽，当输入内容时，自适应文字长度。
- **重构方案**：
  1. **双模式保底默认宽度**：在 [LedgerStyle1Table.tsx] 中加入 `getInputWidth` 宽度自适应计算法，针对数量、单价等数值字段保底预设 75px 宽度，针对供货商、备注、食品索证、经办签名等文本字段保底预设 105px 宽度，确保默认情况亦有足够宽阔的输入视野；
  2. **字符长度动态自适应**：取消输入框的 `w-full` 限制，改为基于输入文字的长度乘以字体占位像素（中文字符占 2 宽，英文字符占 1.15 宽）动态输出样式 `style={{ width: getInputWidth(record.field) }}`。当用户在录入供货商或保质期等长文本时，输入框将平滑地自适应扩宽，撑开对应的表格单元格，大幅度提升表格数据录入体验。

### 2026-07-01 - [V5.15.0] 每日采购细表高对比度样式升级与默认绿色配置
- **需求**：餐位分组下的每日采购细表主题颜色对比度不够明显（单元格、文字、单元格之间界限不易区分），且默认主题需切换为绿色。
- **重构方案**：
  1. **默认绿色主题锁定**：修改 [TableGrid.tsx] 中的初始化默认值，在无 localStorage 主题缓存时，默认主题从 `sky`（天空蓝）调整为 `emerald`（翡翠绿）；
  2. **调深前景色提升文字可读性**：重构了 `THEME_MAP` 颜色常量配置，将四套主题的文字颜色全部加深（如 `text-emerald-700` 提深至 `text-emerald-900`、`text-sky-700` 提深至 `text-sky-900`），金额合计强调色提深至近纯黑的 950 色系（如 `text-emerald-950 font-black`），确保文本即便在浅色背景格衬底中依然有极强对比度；
  3. **网格边框及二级表头大对比度强化**：将表格内部大面积浅淡的 `border-slate-200` 网格细线调深至清晰显眼的 `border-slate-300`，将重要的边界线条调深至 `border-slate-350`；将二级表头（数量/单价/金额）字色由浅灰提深至高对比度的 `text-slate-800 font-semibold bg-slate-100`，从根本上解决了高亮屏幕下网格线不易区分的问题。

### 2026-07-01 - [V5.16.0] 物理首航自动检测清洗浏览器缓存与核心异步防重点击锁屏
- **需求**：
  1. 若系统初次启动，无data数据，用户通过浏览器打开页面时，清空该页面浏览器缓存，防止旧端缓存对新环境脏覆写；若 ./data 下已有数据则不清空。
  2. 当操作需要等待成功时，增加等待锁定效果，避免用户在此期间误操作。
- **重构方案**：
  1. **首航自动物理预检与强力洗清**：在后端的 [/api/storage/load] 接口中，增加对本地物理存储文件 `data/db.json` 是否存在的探针检测。若文件不存在则证明系统是首航初次加载，随接口向前端透传 `isFirstBoot: true`。前端在 App 成功加载前置并行 Promise 响应后，若发现此标志，自动强力清除浏览器 `localStorage` 和 `sessionStorage` 的全量残留垃圾缓存，确保数据源洁净；
  2. **非侵入式 window 级全局锁屏绑定**：在 [App.tsx] 顶层注册了全局 loading 状态和 `(window as any).__setGlobalLoading` 自定义钩子，使子组件和任意异步流程能以极速解耦的方式，随时随地开启/关闭 Loading 等待提示；
  3. **核心高能动作防呆毛玻璃遮罩**：在 [App.tsx] 渲染底层挂载了 z-[9999] 的全屏毛玻璃锁定遮罩，并在 [LedgerSystem.tsx] (保存并同步台账)、[AdminBackend.tsx] (清空当月数据、出厂种子重置、清空台账全库) 等核心异步物理同步方法中全部接入锁屏，在接口响应期间锁定页面交互，为数据安全提供防卫。

### 2026-07-01 - [V5.17.0] 全流程并行同步进度条与防呆操作锁定系统
- **需求**：在需要网络加载和同步数据的地方全部加上同步进度条，防止用户在等待中看到空白，同步未完成时不允许用户执行任何操作。
- **重构方案**：
  1. **首屏数据同步进度条拦截**：在 [App.tsx] 中启用 `isLoading` 并行装载拦截门槛。在首屏异步拉取备餐数据、台账数据、大字典底册期间，将原有的单纯等待动画重构为渐进式的百分比进度条（0% -> 30% -> 60% -> 90% -> 100%），并在 100% 满格同步后延迟 400ms 解除，数据未拉回时强制阻断首页所有点按交互，杜绝初次进入时空表闪烁；
  2. **全局异步遮罩进度跑条增强**：在 [App.tsx] 顶层注册了 `globalLoadingProgress` 百分比。当宿主调用 window 级锁屏方法（如台账保存、大类一键清空、演示种子出厂复位等）时，全屏遮罩会自动注入跑条动画，模拟趋近式进度递增（从 0% 起点在 100ms 内平滑趋近 95%），待最终 Promise.resolve 后直接降级卸载解锁，让同步进度真实可视，杜绝了白屏和等待期重复误点击。

### 2026-07-01 - [V5.17.1] 人数与餐费分析下沉至具体受众辅助工具卡片中
- **需求**：将菜单栏多维度统计决策的【人数与餐费分析】功能，按照不同一级受众拆分到具体餐位分组页面下面，默认隐藏，允许用户按需勾选开启。
- **重构方案**：
  1. **移除独立 ANALYTICS 大 Tab 页面**：从 [App.tsx] 的侧边栏 (Sidebar) 菜单、顶栏聚焦、费用指示牌以及级联删除中，彻底精简移除了 `activeGroup === 'ANALYTICS'` 大页面的逻辑条件与按钮，让受众页更加聚焦；
  2. **下沉至辅助工具卡片**：在 [activeSubTools] 中增加了 `HEADCOUNT: false` 的隐藏初始态。在各一级餐位分组页面的底侧工具栏内，新增了“👥 人数与餐费分析”的开关切换按钮；
  3. **受众自适应渲染**：当用户勾选开启此功能时，将 [HeadcountPanel] 渲染在受众品类表格及 AI 助手的正下方。组件会自适应外部传入的 `activeGroup` 参数，将其就餐人数的录入与单均开销折线统计，完全隔离限制在当前所选的餐位受众内部，极大地提升了决策的相关性与实用性。

### 2026-07-01 - [V5.17.2] 修复 App.tsx 页面裸露注释直接渲染成纯文本的 Bug
- **修复详情**：在剔除大 Tab 级别的三元分支后，`/* 情形 B: 普通受众视图... */` 错误地直接裸露写在了 JSX 的 Container 内，未进行格式包装。现已使用标准的 `{/* */}` JSX 注释结构进行完美合围，消除了该多余非预期的页面文本残留。

### 2026-07-02 - [V5.18.0] 受众与大类管理主键 Key 自动生成与人群头像 Emoji 自定义选择
- **需求**：
  1. 新增一级受众或二级大类时，由系统自动生成唯一值作为 Key 标识，且在新增和编辑时均不允许用户手动输入或修改该 Key；
  2. 一级受众管理的新增和编辑功能要可以自定义选择头像，将该功能重新加入。
- **重构方案**：
  1. **大类与受众主键 Key 自适应生成**：在 [AdminGroupsTab.tsx] 和 [AdminBackend.tsx] 中定义了唯一标识生成器（形如 `GROUP_XXX_NN` 与 `CAT_XXX_NN`）。不论是新增还是编辑表单，Key 输入框均被强制设为 `disabled={true}`（灰色只读状态），完全堵截了用户在客户端手动输入、错拼或修改 Key 从而造成父子节点断裂的危险；
  2. **一级人群头像自定义选择**：在一级受众管理表单内加回了“图标/头像 Emoji”的下拉选择器。用户在新增或编辑人群时，可自由为受众分配特定的表情符号（例如：🏫 教师、👶 幼儿、🏢 职工、👦 学生等），极大提升了侧边栏和底卡在视觉表达层面的亲和度与定制性；
  3. **生命周期完美重置**：重构了表单提交成功、清空数据、以及取消/删除触发的状态回收机制。重置表单时，系统会瞬间重新生成新的合法唯一 Key 并填装入只读框内，实现了天衣无缝的极速连贯输入体验。

### 🚀 2026-07-02 - [V5.19.0] 物理打印出库单样式与内容布局定制化升级
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次针对出库单物理打印模版的布局设计、字体宋体（SimSun）、字号大小、无黑色边框的标题与日期行，以及二级分类下“出库人”与“接收人”的跨行 `rowSpan` 合并进行了修改。本次优化仅属于前端打印样式的视觉及布局渲染，AI 专用的各种膳食诊断、采购与成本审计提示词没有任何颠覆性修改，100% 保持无缝绿灯兼容。

### 🚀 2026-07-02 - [V5.30.0] 中老年人关怀字号及大按钮模式支持（三档调节）
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次重构为系统添加了标准、大、超大三档字体与按钮尺寸调节。通过在 App 根端挂载字号 class 并动态控制 html font-size 的物理 rem 基数，实现了全系统的等比无损放大。此调整全部局限在前台视障/老龄关怀的 CSS 级视觉显示缩放层，AI 诊断提示词并无任何修改，保持 100% 绿灯兼容。

### 🚀 2026-07-02 - [V5.31.0] 彻底删除AI膳食成本规划及三大统计图表/人数餐费分析功能，同步下线服务端Gemini提示词集成
- **提示词变更状态**：**有变动（提示词文件整体下线）**。
- **说明**：应管理员要求，彻底删除”日开支走势”、”品类资金占比”、”重点监控”、”AI后厨精细化膳食与成本规划”、”人数与餐费分析”五个功能及其全部相关代码。由于 [prompts.ts] 中的 `AI_PROMPTS`（系统角色定位提示词 `systemInstruction` 与结构化分析提示词模板 `analysisPromptTemplate`）仅服务于已删除的”AI后厨精细化膳食与成本规划”功能，随该功能一并整体删除该文件；服务端 [server.ts] 中调用这些提示词的 `/api/gemini/analyze` 接口、`getGeminiClient` 客户端初始化逻辑及 `@google/genai` 依赖包也一并移除。前台同步删除 [StatisticsPanel.tsx]、[AiAssistant.tsx]、[HeadcountPanel.tsx] 三个组件及 [App.tsx] 中对应的引入、状态与渲染分支，`recharts`、`react-markdown` 两个仅服务于被删组件的依赖包也一并从 package.json 中卸载。本次为纯功能下线，此前各版本记录的提示词内容作为历史存档继续保留在本文件靠前的章节中不做删改，仅在此追加说明该提示词现已随功能一起停用。

### 🚀 2026-07-02 - [V5.32.0] 彻底删除管理后台”数据维护核销”Tab 及其全部关联代码
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次删除管理配置后台的”数据维护核销”Tab（JSON备份导出/导入、CSV汇总导出、清空/恢复出厂种子等 7 个数据维护操作）及其在 [AdminBackend.tsx]、[App.tsx]、[store.ts]、[ledgerStore.ts]、[utils.ts] 中的全部关联代码，属于纯管理端功能与数据层方法的下线清理，未涉及任何 AI 提示词或 `src/prompts.ts` 的改动，保持 100% 绿灯兼容。分支说明：本次改动固定在新建的 `dev` 分支上，未合并进 `main`（`main` 分支保留其独立的台账打印样式修复历史）。

### 🚀 2026-07-02 - [V5.33.0] 全项目已删除功能遗留代码地毯式清理
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯代码清理，删除了此前几次功能下线（人数与餐费分析、数据维护核销等）后残留的死代码，包括 `dailyHeadcount` 数据字段及配套初始化逻辑、`updateHeadcount`/`fetchAllReports`/`validateImportedReport` 等零调用方法、孤儿组件文件 [LedgerAddMaterialInlineForm.tsx]、多处未使用的 import 引入及未使用的 `motion` 依赖包。未涉及任何 AI 提示词或 `src/prompts.ts` 的改动。清理前均先用 grep 在全项目范围确认零调用方才执行删除，共享的私有种子生成方法与确认弹窗逻辑均予以保留未动。











### 🚀 2026-07-02 - [V5.34.0] 修复原料字典历史脏数据引发的 React 重复 Key 警告（黄瓜/土豆）
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端数据服务层 Bug 修复，承接 V5.33.0 排查时登记的独立跟进任务，未涉及任何 AI 提示词或 `src/prompts.ts` 的改动。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
在 KPMSS（canteen ledger app）中，观察到两处 React 控制台重复 key 警告：
1. 管理后台 → 原料字典管理 tab（AdminDictTab.tsx，数据源 RawMaterialsDictService）：
   "Encountered two children with the same key... 黄瓜" 与 "...土豆"，反复出现。
2. 原料购销台账 → 任选台账 → 点击"开启今日录入"进入录入模式（LedgerSystem.tsx）：
   "Encountered two children with the same key... temp_黄瓜" 与 "...ledger_item_KID_..."。

# 排查要求
(a) 检查 RawMaterialsDictService.getItems() / 字典数据本身是否存在字面重复的同名条目；
(b) 检查 AdminDictTab.tsx 的列表 key 是按 name 还是 id 生成；
(c) 检查 LedgerSystem.tsx 录入模式下 "temp_" 前缀键的生成逻辑；
要求修复根因（去重字典数据 / 改用真正唯一 ID 作为 key / 阻止新增重复原料名），
并通过复现两处症状、确认控制台清零、确认正常原料不受影响来验证修复。
```

- **排查结论**：根因为 [rawMaterialDict.ts] 的 `RawMaterialsDictService.initDictFromServer()` 从服务器加载 `rawMaterialsDict` 时未做同名去重，历史脏数据（同名不同来源的重复原料条目）被直接载入内存；本分支 `generateDefaultSeeds()` 默认种子数据本身也字面重复注册了「黄瓜」「土豆」两条，是首航必现该 Bug 的最直接根因。脏数据/重复种子被 [AdminDictTab.tsx]（`key={item.name}`）、[LedgerSystem.tsx]（`temp_${dictItem.name}` 临时 ID 合并）、原料联想下拉框（`dictOptions`）三处渲染点同时复用，导致重复 key。
- **修复方案**：详见 [readme_zh.md] 中 [V5.34.0] 条目——在 `RawMaterialsDictService` 新增 `dedupeByName()`，于两个批量赋值入口（`initDictFromServer`、`setRawMaterialsDictInMemory`）统一去重；直接删除种子数据中末尾冗余重复的「黄瓜」「土豆」两条；并在 [syncHelper.ts] 新增 `SyncHelper.runWhenInitialized()` 一次性回调队列，让去重后的清洗结果能在系统初始化解锁后安全回写服务器，实现持久化数据自愈。

### 🚀 2026-07-02 - [V5.35.0] 项目代码结构、数据流与持久化机制全面重构，并补齐架构文档与文件级注释
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯代码结构重构，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
分析项目的代码结构，数据流向，保存系统。重构代码，把重构后的项目设计落实到文档，
使得代码结构更清晰，给每个代码文件加上顶行注释说明文件主要功能。
```

- **重构结论**：详见 [readme_zh.md] 中 [V5.35.0] 条目与新建的 [ARCHITECTURE.md]——前端按业务域重组目录（`services/`、`types/`、`constants/`、`hooks/`、`components/{admin,ledger,inventory,shared}`）；从 [App.tsx]、[LedgerSystem.tsx]、[TableGrid.tsx] 抽取 6 个自定义 Hook 及 `HelperSelect`/`SensorySelector` 两个共享组件；`server.ts` 拆分为 `server/storageService.ts`、`server/logService.ts`、`server/routes/{storage,misc}.ts`；全部 45 个源码文件补齐顶部 `@description` 说明注释。项目当前已不含任何 AI 功能（AI 后厨膳食规划已于 V5.31.0 整体下线），本次重构与 AI 提示词管理机制无任何交叉。

### 🚀 2026-07-03 - [V5.36.0] 感官性状展示框自适应宽度修复及台账表格左右移动导航按钮
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端 UI 修复与交互增强，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
台账数据录入界面，多选完感官性状后，选择描述要展示在对应的展示框里。调整字段的宽度，
最窄不能小于两列，自适应文字内容。设置左右移动的按钮，当一行记录太长的时候方便用户
左右移动记录，查找对应内容。
```

- **修复方案**：详见 [readme_zh.md] 中 [V5.36.0] 条目——根因是 [SensorySelector.tsx] 展示框此前用 `w-full` 百分比宽度，无法向 `table-layout: auto` 的原生表格反馈列所需宽度，导致多选后内容被裁切看不见；修复为按选中内容动态计算显式 px 宽度并设最小宽度（两列常规输入框宽度），同时在 [LedgerStyle1Table.tsx]、[LedgerStyle2Flow.tsx] 新增 sticky 定位的左右移动导航按钮，方便纵向滚动长表格时随时横向移动查看。

### 🚀 2026-07-03 - [V5.37.0] 台账新增"当前同步日期无记录"提示，避免误判数据丢失
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端问题排查与 UI 提示增强，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
当前幼儿和在校生在采购细表有金额和数量，但是对应的台账和出入库记录都什么都没有。
分析原因并解决这个问题。
```

- **排查结论**：详见 [readme_zh.md] 中 [V5.37.0] 条目——[App.tsx] 的 `TableGrid`（采购细表）是整月矩阵视图，所有日期同时可见；而 [LedgerSystem.tsx] 的台账视图是"单日快照"，只显示 `selectedDate` 这一天，且默认取系统当前日期。经核对磁盘文件 `data/ledgers/daily/2026/07/02.json` 及 `/api/storage/load` 实际返回内容，确认数据全部正确落盘且能正确读取，记录日期为 2026-07-02，但系统当前日期已推进到 2026-07-03，用户未切换日期导致看到空表，误以为数据丢失，并非真正的数据丢失或同步失败。
- **修复方案**：在 [LedgerSystem.tsx] 新增 `nearestRecordDateHint` 计算属性，非录入模式下若当前日期无记录则提示"并非数据丢失"并提供"跳转查看该日期"一键切换按钮，录入模式下自动隐藏不影响正常录入工作流。

### 🚀 2026-07-03 - [V5.38.0] 台账录入操作栏消除按钮左侧留白并压缩纵向留白，扩大记录显示区域
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端布局与交互优化，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
台账录入界面，开启今日录入的按钮左侧有大片留白，调整按钮的层级、位置和顺序，
让用户使用更简易方便。尽量让台账记录的显示区域更大一些，让用户更好查看。
```

- **修复方案**：详见 [readme_zh.md] 中 [V5.38.0] 条目——根因是 [LedgerControlBar.tsx] 操作行用 `justify-between` 把常为空的左侧筛选占位与按钮组分居两端，样式一下留白约 629px；修复为按钮组固定排在最左侧（`order-1`），样式二筛选器移到其后（`order-2`），并收紧页眉、操作栏、[LedgerStyle1Table.tsx] 筛选/标签工具条的内边距（合并两行为一行、删除重复提示文案），实测表格可视区域从 520px 提升至 544px。

### 🚀 2026-07-03 - [V5.39.0] 后端日志系统支持按日期+文件体积自动归档
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯后端日志持久化机制改造，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
日志系统的本地日志按时间和文件大小自动归档，避免单个文件过大。
用户的所有台账记录，也要按年月日归档，避免存档过大，也可以方便用户保存和查看历史记录。
```

- **澄清结论**：经与用户确认，台账记录归档部分在 [V5.29.0] 已实现（逐日流水拆分至 `data/ledgers/daily/年/月/日.json`），用户确认现状已满足，本轮无需新增功能，仅需处理日志系统部分。
- **修复方案**：详见 [readme_zh.md] 中 [V5.39.0] 条目——[logService.ts] 日志文件改为按日期归档至 `data/logs/app-YYYY-MM-DD.log`，同一天内体积超过上限（默认 5MB）时自动滚动到序号分片文件，并对升级前遗留的 `data/app.log` 做了平滑迁移。

### 🚀 2026-07-03 - [V5.40.0] 支持单机离线部署：可配置端口、生产模式免依赖Vite、外部CDN资源异步化
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯工程化部署改造，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
客户目前方案是选择本地部署使用，不联网。有什么注意事项？我这面开发完毕，
如何让用户以最简单的方式单机部署使用本系统。
端口可能会有冲突，让用户可以手动设置启动端口。
```

- **修复方案**：详见 [readme_zh.md] 中 [V5.40.0] 条目——[server.ts] 端口改为读取环境变量 `PORT` 且修正了开发/生产模式判定逻辑（必须显式 `NODE_ENV=development` 才进开发模式），[package.json] 修正了 vite 相关构建工具的依赖分类并新增 `cross-env`，[index.html] 把外部字体与 twemoji 表情脚本改为异步非阻塞加载，新增 [start-windows.bat]、[start-mac-linux.sh] 一键启动脚本与 [部署指南.md] 部署文档。

### 🚀 2026-07-03 - [V5.41.0] 台账总表模式只显示当天有出入库记录的原料
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端展示逻辑修复，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
在台账录入界面，当在某天录入过某一原料后，再选择其他天时，该原料也会显示出来，
这看起来怪。该天没有采购的原料不要显示出来。
```

- **修复方案**：详见 [readme_zh.md] 中 [V5.41.0] 条目——[LedgerSystem.tsx] 的 `filteredLedgerItems` 新增按 `selectedDate` 是否存在出入库记录的过滤条件（仅非录入模式生效），[LedgerStyle1Table.tsx] 同步优化了空列表的提示文案。

### 🚀 2026-07-03 - [V5.42.0] 默认生成的一级受众/二级品类/原料仅允许编辑，禁止删除
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为数据保护类功能新增，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
默认生成的一级受众和默认生成的二级品类和默认生成的原料不允许用户进行删除，
可以进行编辑。
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.42.0] 条目——`DynamicGroup`/`DynamicCategory`/`RawMaterialDictItem` 新增 `isDefault` 标记；[store.ts]、[rawMaterialDict.ts] 的删除方法新增服务层守卫拒绝删除默认数据，编辑方法保留标记；针对升级前已生成、缺少标记的历史数据做了一次性自动迁移补齐；[AdminGroupsTab.tsx]、[AdminBackend.tsx]、[AdminDictTab.tsx] 三处管理界面为默认数据项新增"默认"标签与锁形图标替代删除按钮。

### 🚀 2026-07-03 - [V5.43.0] 清理遗留死代码：删除 TARGET_GROUP_LABELS、RAW_MATERIALS_DICT_KEY，补齐 SyncHelper 缺失的类型声明
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯代码清理与类型声明修正，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
（针对贴出的 TARGET_GROUP_LABELS 代码片段）这是干嘛的，有什么用么？
删除掉，项目还没有部署，还在自测阶段，不需要考虑旧表残留的问题。
除此之外是否还有其他类似的遗留无用代码。
```

- **排查与修复方案**：详见 [readme_zh.md] 中 [V5.43.0] 条目——确认 `TARGET_GROUP_LABELS` 是动态人群重构前的遗留静态映射表后予以删除；全项目 grep 排查后另确认 [rawMaterialDict.ts] 的 `RAW_MATERIALS_DICT_KEY` 同属遗留死代码一并删除；额外发现并修复了 [syncHelper.ts] 里 `debounceTimer` 缺失类型声明的遗留疏漏（非死代码，是真实使用但漏声明的属性），使 `tsc --noEmit` 报错数从 29 降至 26。

### 🚀 2026-07-03 - [V5.44.0] 全局界面文字增大一号，压缩布局留白，扩大核心数据表可视区域
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端展示层调整，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
系统的界面文字全部增大一号，布局紧凑一些，不同区域之间留白太多，整体布局
紧凑好腾出空间来更好的展示餐位分组的每日采购细表和台账数据录入界面的
原料购销日总表明细。
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.44.0] 条目——[App.tsx] 共享外壳、[TableGrid.tsx] 系列（备餐采购细表）、[LedgerSystem.tsx] 系列（台账数据录入）内全部文字按自定义像素字号/Tailwind 标准字号各上调一级，同时收紧对应容器的内边距与间距抵消文字变大占用的空间，净效果是核心数据表格可视区域变大。

### 🚀 2026-07-03 - [V5.45.0] 修复台账录入保存后供货商/采购员等字段偶发显示为空的竞态问题
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯 Bug 排查与修复，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
台账数据录入界面，"供货商及地址，采购员，检验员，保管员，出库人，接收人"
选择完内容填入保存后，要展示在对应的输入框中。现在保存后输入框中没有内容。

台账数据录入界面，土豆的"供货商及地址，采购员，检验员，保管员，出库人，
接收人"这几项都有内容，但是保存后显示的都是空的
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.45.0] 条目——定位到心跳静默同步与录入保存之间的竞态问题，在 [syncHelper.ts] 记录最近一次本地写入时间戳，[useAppData.ts] 的心跳同步据此丢弃与本地写入竞态的过期响应；顺带修复了 [LedgerStyle1Table.tsx] 中此前 sed 批量替换遗留的占位符问题。

### 🚀 2026-07-03 - [V5.46.0] 修复台账HelperSelect禁用态忽略宽度导致字段内容不可见的问题
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯 Bug 排查与修复，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述（配图反馈），供后续追溯：

```markdown
# 用户原始诉求
如图所示，不显示内容不是偶发的，就是不显示，英国类似感官性状的现实逻辑，
选择输入完之后直接显示在输入框里
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.46.0] 条目——定位到 [HelperSelect.tsx] 禁用态输入框硬编码 `w-full` 而忽略了调用方传入的宽度 `className`，导致表格自动布局算法在整列均为禁用态时缺少宽度基准而坍缩，属于纯 CSS 渲染缺陷，与 [V5.45.0] 修复的心跳同步竞态是两个独立问题。

### 🚀 2026-07-03 - [V5.47.0] 台账总表"二级品类"列改为按实际内容自适应宽度
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端展示层调整，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述（配图反馈），供后续追溯：

```markdown
# 用户原始诉求
如图，二级品类要以实际的内容做自适应
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.47.0] 条目——[LedgerStyle1Table.tsx] 去掉"二级品类"列写死的 `w-20` 固定宽度，改为 `whitespace-nowrap` 让列宽按实际内容自适应，解决表头文字被迫竖排换行的问题。

### 🚀 2026-07-03 - [V5.48.0] 修复台账打印勾选空品类时报错，改为禁止勾选无数据大类并兜底空表预览
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯 Bug 排查与修复，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
在打印总表模式（图一）下的记账登记表，如果所选品类记录为空，会报错！
应该没有品类就不让勾选。就算选择了，也不能直接报错，应该是预览空表
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.48.0] 条目——定位到 [LedgerPrintStyle1.tsx] 中引用了未导入常量 `LEDGER_PRINT_OUT_CONFIG` 的笔误并修正；[LedgerSystem.tsx]/[LedgerPrintModal.tsx] 新增"本台账实际有数据的大类"计算，禁用无数据大类的勾选框。

### 🚀 2026-07-03 - [V5.49.0] 台账打印总表模式仅勾选"低耗品"时改用消耗品出入库台账专属样式
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端展示层新增功能（打印模板分支），未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述（配图反馈），供后续追溯：

```markdown
# 用户原始诉求
台账数据录入界面，打印总表模式（图一）下的记账登记表时，当只勾选消耗品
时，打印样式要和其他的不一样。如图所示
我解释下里面的某些字段，规格就是原料备注里面的内容，入库经手人就是采购
员，出库经手人就是出库人，再加上库存和备注。
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.49.0] 条目——新增 [LedgerPrintStyle1Consumable.tsx] 消耗品专属打印模板，[LedgerPrintStyle1.tsx] 在仅勾选"低耗品"时委托给该组件渲染，其余组合维持原样式不变；顺带修复了一处因新增运行时枚举访问而暴露的 `FoodCategory` 导入错误。

### 🚀 2026-07-03 - [V5.50.0] 消耗品专属打印样式从总表模式（图一）迁移到单原料日流水（图二）
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端展示层调整（撤销上一次的图一特殊样式，改为在图二按原料大类判断），未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述（配图反馈），供后续追溯：

```markdown
# 用户原始诉求
修改低耗品的打印功能。台账数据录入界面，打印单原料日流水图二）下的记账
登记表时，当选择的采购项目是消耗品时，打印样式要和其他的不一样。如图所
示。
我解释下里面的某些字段，规格就是原料备注里面的内容，入库经手人就是采购
员，出库经手人就是出库人，再加上库存和备注。
同时，把上次任务的"打印总表模式（图一）下的记账登记表时，当只勾选消耗
品时，打印样式要和其他的不一样"这个功能取消掉，还是和以前一样。
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.50.0] 条目——撤销 [LedgerPrintStyle1.tsx] 的特殊分支并删除 [LedgerPrintStyle1Consumable.tsx]；新增 [LedgerPrintStyle2Consumable.tsx]，在 [LedgerPrintStyle2.tsx] 按当前选中原料的大类判断是否委托渲染。

### 🚀 2026-07-03 - [V5.51.0] 清理台账系统"手动新增台账"整条遗留死代码链
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯代码清理，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
（贴出 PRESET_LEDGER_MATERIALS 代码片段）是否是遗留代码，有什么用处
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.51.0] 条目——确认 `PRESET_LEDGER_MATERIALS` 是分组联动同步重构前"手动新增台账"表单的种子数据，而该表单在 UI 上从未被实际渲染/绑定，属于完全不可达的死代码链，予以整条删除。

### 🚀 2026-07-03 - [V5.52.0] 清理 LedgerSystem.tsx 内多个已废弃的函数/状态/组件
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯代码清理与文案修正，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
检查LedgerSystem.tsx文件，里面可能有一些函数或者常量变量是被废弃的
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.52.0] 条目——清理三处纯死代码（`daysArray`/`handleDeleteLedger`/`handleCellBlur`），并删除整条断链的"新增原料"弹窗子系统（含 `LedgerAddMaterialModal.tsx` 文件），同时把两处过时的空态提示文案改为指引用户使用现在实际生效的"开启今日录入"路径。

### 🚀 2026-07-03 - [V5.53.0] 重新设计顶部横幅/页脚按钮层级，去除面向用户无意义的技术文案，台账样式切换按钮前移
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端展示层调整（UI 层级重设计、文案精简、按钮重新排序），未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述（配图反馈），供后续追溯：

```markdown
# 用户原始诉求
如图所示，红色框选内部按钮和文字看起来乱，从设计角度重新设计按钮的层级
和位置，去掉没必要给用户在系统首页展示的文字。
"总表模式（图一）"和"单原料日流水（图二）"的按钮位置不合逻辑，修改这两
个按钮的层级，让这两个按钮在"开启今日录入"的前面。其余不变。要适配不同
分辨率和环境。
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.53.0] 条目——顶部横幅去掉"物理热同步"并用分隔线分组三类按钮；页脚去掉"操作权限"与"HOST/SYSTEM_HOT_CONNECTED"两处调试信息，顺带修复了页脚在台账模块显示"undefined"的既有 bug；台账样式切换按钮移到"开启今日录入"之前。

### 🚀 2026-07-03 - [V5.54.0] 开启今日录入自动切回总表模式，台账原料累计入库支持本月/全部切换
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为纯前端功能增强，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
点击开启今日录入后，自动将页面跳转到"总表模式（图一）"。"台账原料累计入
库"处默认显示当前月的累计入库，用户也可以选择切换为所有记录的累计入库
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.54.0] 条目——[LedgerSystem.tsx] 的 `handleStartRecording` 包装为先切样式再进入录入；[App.tsx] 新增按月过滤的统计与 `ledgerAmountScope` 状态，配合"本月/全部"切换按钮。

### 🚀 2026-07-03 - [V5.55.0] 引入 Vitest 全功能自动化测试体系，规避业务逻辑层静默失效风险
- **提示词变更状态**：**无变动 (No Changes)**。
- **说明**：本次为测试基础设施建设，未涉及任何 AI 提示词。此处按惯例同步记录本次改动所依据的原始需求描述，供后续追溯：

```markdown
# 用户原始诉求
添加全功能、全覆盖的自动化测试，保证每次修改完功能正常，无崩溃，无报错。
后续功能添加的时候就附带上对应自动化测试。
```

- **实现方案**：详见 [readme_zh.md] 中 [V5.55.0] 条目与 [ARCHITECTURE.md] 新增的"七、自动化测试体系"章节——新增 Vitest + Testing Library + supertest 测试基础设施，业务逻辑层（services/hooks/utils/后端）100% 覆盖 + 9 个关键交互组件，共 23 个测试文件、318 个用例；过程中发现并修复了 4 个此前从未被手工测试触发过的真实缺陷（2 个前端崩溃风险、1 个级联更新失效、1 个路径穿越安全漏洞）。
