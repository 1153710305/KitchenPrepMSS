# 单机离线部署指南

本文档面向"客户单机部署使用、不联网"这一使用场景，说明部署前的注意事项、最简单的部署步骤，以及日常运维建议。

---

## 一、离线部署的核心注意事项

### 1. 系统本身不依赖任何在线服务，可安全离线运行
- 数据持久化默认是 **本地文件模式**（`.env` 中 `STORAGE_TYPE="local"`），全部数据保存在本机 `data/` 目录下的 JSON 文件里，不需要连接任何数据库或云服务。
- 系统内已不含任何 AI 联网调用（AI 膳食规划功能已在早期版本整体下线），不会有任何"因联网请求超时导致功能异常"的情况。
- 页面本身（`index.html`）原本引入了 Google 网络字体与 twemoji（旧版 Windows 兼容用的彩色 emoji 图标）两个外部 CDN 资源。这两处已改为**异步非阻塞加载**：有网络时会自动增强显示效果，完全离线时会在几乎无感知延迟的情况下自动回退为系统自带字体（微软雅黑等）和系统原生 emoji 字符，不会造成页面卡顿或白屏。**这是本次专门为离线部署做的修复**，之前的版本这两处是阻塞式加载，在真正断网环境下会明显拖慢首次打开页面的速度。

### 2. 离线机器上不能执行 `npm install`
`npm install` 需要连接 npm 官方仓库下载依赖包，断网环境下无法执行。因此正确流程是：**在一台能联网的电脑上完成安装和构建，再把完整的项目文件夹拷贝到客户的离线电脑上**，离线电脑上不需要也不应该再执行 `npm install`。具体步骤见下方"二、部署步骤"。

### 3. 离线电脑仍需要预先安装 Node.js 运行环境
Node.js 是运行本系统后端服务所必需的运行时，与 `npm install` 的网络依赖是两回事——Node.js 只需要**安装一次**，装好之后本身不需要联网。做法：
- 在能联网的电脑上，从 [nodejs.org](https://nodejs.org) 下载 Windows/macOS 对应的离线安装包（`.msi`/`.pkg`），拷贝到 U 盘，在客户电脑上双击安装即可，全程无需客户电脑联网。
- 建议安装 **Node.js 18 或更高的 LTS 长期支持版本**。

### 4. 数据备份需要客户自行定期做好本地/异地备份
由于是纯本地文件存储，**没有任何云端自动备份**，一旦客户电脑硬盘损坏或误删 `data/` 目录，数据将无法找回。系统本身有一套自动快照机制（每次保存数据时都会在 `data/backups/` 下生成一份带时间�戳的快照，最多保留最近 30 份），但这只能防止"最近误操作"，防不了硬盘物理损坏。**强烈建议客户定期（如每周）把整个 `data/` 目录拷贝到 U 盘或另一台电脑做异地备份。**

### 5. 局域网多人访问场景下的权限提示
如果客户是"一台电脑当服务器、局域网内其他电脑通过浏览器访问"的用法（而不是完全单机单人使用），请注意：当前登录密码校验只在浏览器前端进行，服务端接口本身没有二次鉴权。这意味着能访问到服务器所在局域网的人，理论上可以绕过登录页直接调用数据接口。如果客户的使用场景是**完全独立的单机、仅本机浏览器访问**，这个风险基本不存在；如果是**局域网内多人共享访问**，建议后续评估是否需要增加服务端鉴权（这属于额外的安全加固需求，不在本次部署范围内，如有需要可以后续单独排期处理）。

### 6. 首页与管理后台密码目前是明文写在 `.env` 里、且会被打包进浏览器可读的前端文件中
这两个密码仅用于防止普通员工误触修改配置，不是面向互联网攻击者设计的强安全机制，对完全离线单机场景是合理够用的；但**不要把这套密码机制当作能防御"内部人员用浏览器开发者工具查看源码"级别的安全边界**。如果对保密性有更高要求，同样属于后续可选的安全加固项。

---

## 二、部署步骤（最简单流程）

### 第一步：在有网络的电脑上准备安装包（开发者/实施人员操作一次即可）

```bash
# 1. 安装依赖（需要联网）
npm install

# 2. 构建生产版本（生成 dist/ 目录）
npm run build
```

构建完成后，确认以下内容都存在：
- `dist/`（前端静态文件 + 后端可执行的 `server.cjs`）
- `node_modules/`（运行所需的依赖包，必须一并拷贝，客户电脑上不会再执行 `npm install`）
- `package.json`
- `.env`（正式部署前请检查确认里面的端口、密码等配置，见下方"三、常用配置"）
- `data/`（如果已经有需要预置的初始数据，一并带上；否则留空，首次启动会自动生成默认数据）
- `start-windows.bat` / `start-mac-linux.sh`（本次新增的一键启动脚本）

> 提示：`src/`、`server/` 等源代码目录、以及 `.git` 版本库目录，客户电脑上并不需要运行，可以不拷贝以减小体积（但保留也不影响运行）。

### 第二步：拷贝到客户电脑
把上面这些文件（整个项目文件夹）通过 U 盘或局域网共享的方式，完整拷贝到客户的离线电脑上，放在任意一个目录下即可（不要放在有中文特殊符号或权限受限的系统目录下，建议放在如 `D:\食堂管理系统\` 这样的普通目录）。

### 第三步：客户电脑上安装 Node.js（仅需一次）
如果客户电脑之前从未装过 Node.js，参考上方"一、注意事项 3"，用离线安装包装好即可。

### 第四步：启动系统
- **Windows**：双击项目文件夹里的 `start-windows.bat`。
- **macOS / Linux**：双击（或在终端执行 `./start-mac-linux.sh`）项目文件夹里的 `start-mac-linux.sh`。

脚本会自动检测 Node.js 是否安装、`dist/` 是否构建完成，然后启动服务并在窗口里提示访问地址（默认 `http://localhost:3000`）。**只要这个窗口保持打开，服务就一直在运行；关闭窗口即代表服务停止。**

### 第五步：打开浏览器使用
在客户电脑上打开 Chrome/Edge 等现代浏览器，访问启动脚本提示的地址（如 `http://localhost:3000`）即可开始使用，首页登录密码见 `.env` 中的 `VITE_LOGIN_PASSWORD`。

---

## 三、常用配置（`.env` 文件）

| 配置项 | 说明 | 默认值 |
|---|---|---|
| `PORT` | 服务监听端口。**如果启动时提示端口被占用（常见于电脑上已有其他软件占用了 3000 端口），修改这一项为其他数字（如 `8080`）后重新启动即可，无需改动任何代码。** | `3000` |
| `VITE_LOGIN_PASSWORD` | 首页登录密码 | `guest` |
| `VITE_ADMIN_PASSWORD` | 管理后台密码 | `admin` |
| `STORAGE_TYPE` | 存储模式，离线单机部署请保持 `local` | `local` |
| `LOCAL_DB_PATH` | 主数据文件路径 | `data/db.json` |
| `LOCAL_LOG_DIR` | 日志归档目录（按日期自动分文件） | `data/logs` |
| `LOG_MAX_FILE_SIZE_MB` | 单个日志文件体积上限（MB），超出后自动滚动到下一个文件 | `5` |

修改 `.env` 后需要**重新启动**（关闭启动窗口再重新运行启动脚本）才能生效。

---

## 四、常见问题排查

- **双击启动脚本一闪而过 / 提示找不到 Node.js**：说明尚未安装 Node.js，参考"一、注意事项 3"安装后重试。
- **提示找不到 dist/server.cjs**：说明拷贝时漏掉了 `dist/` 目录，或者忘记在联网电脑上执行 `npm run build`。
- **提示端口被占用（`EADDRINUSE`）**：修改 `.env` 里的 `PORT` 为其他端口号（如 `8080`）后重新启动。
- **浏览器打不开页面**：确认启动窗口里显示了 `[KITCHEN SYSTEM LIVE] 服务就绪!` 字样再访问；确认浏览器地址栏里的端口号和 `.env` 里 `PORT` 一致。
- **数据似乎"丢失"了**：先检查是不是选错了日期/受众分组，本系统很多数据是按日期归档展示的，不同日期查看内容不同，并非真的丢失；确需恢复历史数据时可在管理后台或 `data/backups/` 目录下找最近的自动快照。
