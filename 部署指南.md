# 食堂用餐服务管理系统 —— 安装部署与使用手册

本手册覆盖两种部署场景：**客户单机离线部署**（不联网，交付给食堂/学校现场使用）与 **云服务器部署**（联网，供多人通过浏览器远程访问）。两种场景的构建步骤基本一致，差异主要在"部署到哪、怎么保活、怎么对外暴露"。请先看下面的选型对照表，确认自己要做的是哪一种，再跳到对应章节。

---

## 一、两种部署方式怎么选

| | 客户单机离线部署 | 云服务器部署 |
|---|---|---|
| **适用场景** | 客户现场一台电脑，不联网或不需要联网，本机浏览器（或局域网内几台电脑）访问 | 部署在云厂商的一台虚拟机/服务器上，通过公网 IP 或域名供多地用户访问 |
| **数据存储** | 本地 SQLite 文件（`data/kpmss.sqlite`），装在客户电脑本地磁盘 | 可选：本地 SQLite（挂载云盘持久化）或腾讯云 COS 对象存储模式 |
| **进程保活** | 手动双击启动脚本，窗口常驻即为运行中，关窗口即停止 | 需要用 `pm2` 或 `systemd` 之类的进程守护工具，实现开机自启、崩溃自动重启 |
| **对外暴露面** | 仅本机/局域网，几乎不暴露给公网攻击者 | 直接暴露在公网，**必须做安全加固**（详见四、11 节），否则风险显著更高 |
| **HTTPS** | 不需要（本机 `http://localhost` 或局域网内网访问） | 建议必须做（域名 + Let's Encrypt 免费证书，或云厂商托管证书） |
| **本手册对应章节** | 第三章 | 第四章 |

两种方式的**第一步都相同**：先在一台联网的电脑上准备好完整的部署包，见下面第二章。

---

## 二、通用准备工作：构建部署包

无论部署到客户单机还是云服务器，都需要先在一台**联网**的电脑上把项目构建成可运行的产物，再把产物搬到目标机器。

### 1. 关键前提：Node.js 版本必须与目标机器完全一致

本系统本地存储所用的 `better-sqlite3` 是一个**原生二进制模块**（项目里唯一的非纯 JS 依赖），它的预编译二进制**按 Node.js 大版本号严格绑定**——换一个 Node 大版本运行会直接报错 `ERR_DLOPEN_FAILED`，不是"能不能跑快慢"的问题，是完全跑不起来。这意味着：

- **构建部署包的电脑，必须安装与目标机器完全相同的 Node.js 大版本**——本项目统一约定为 **22.x**（`package.json` 的 `engines` 字段已声明这一要求）。
- 如果构建电脑和目标机器的**操作系统或 CPU 架构不同**（比如在 Mac 上构建、要部署到 Windows 客户电脑，或在 Mac 上构建、要部署到 Linux x86_64 云服务器），必须在与目标系统一致的机器/虚拟机/容器里重新执行一次 `npm install`，确保 `node_modules/better-sqlite3` 里下载到的预编译二进制是给目标平台用的——**不能跨操作系统直接拷贝 `node_modules`**。
  - 部署到 **Linux 云服务器**：最简单的办法是直接在一台同架构（通常是 x86_64）的 Linux 环境里执行 `npm install && npm run build`（可以用 Docker 容器、云服务商提供的构建机、或者干脆直接在目标云服务器上装好 Node 22.x 后现场跑一次 `npm install && npm run build`——云服务器本身是联网的，不像客户单机那样必须离线交付，这一步可以直接在云服务器上做，反而更简单，见第四章）。
  - 部署到 **客户离线单机**：客户电脑不联网，必须在别的地方构建好再整体拷贝过去，见第三章。

### 2. 构建命令

```bash
# 0. 先确认当前电脑安装的是 Node.js 22.x
node --version

# 1. 安装依赖（需要联网）
npm install

# 2. 构建生产版本
npm run build
```

构建完成后应包含以下产物，确认都存在：
- `dist/`（前端静态文件 + 后端可执行的 `server.cjs`）
- `node_modules/`（运行所需依赖，`node_modules/better-sqlite3` 含针对当前系统编译好的原生二进制）
- `package.json`
- `.env`（正式部署前务必检查确认端口、密码等配置，见 §3.3 / §4.6）
- `data/`（如果有需要预置的初始数据可一并带上；留空则首次启动自动生成默认数据）
- `start-windows.bat` / `start-mac-linux.sh`（仅客户单机部署需要）

> 提示：`src/`、`server/` 等 TypeScript 源代码目录以及 `.git` 版本库目录，运行时并不需要，可以不搬到目标机器以减小体积（保留也不影响运行）。

---

## 三、方式 A：客户单机离线部署

### 3.1 部署步骤

**第一步（联网电脑上做一次）**：按第二章完成构建。

**第二步：拷贝到客户电脑**
把构建产物（整个项目文件夹）通过 U 盘或局域网共享方式，完整拷贝到客户的离线电脑上，放在任意目录下（避免中文特殊符号或权限受限的系统目录，建议 `D:\食堂管理系统\` 这样的普通目录）。

**第三步：客户电脑上安装 Node.js（仅需一次）**
如果客户电脑之前从未装过 Node.js：在联网电脑上从 [nodejs.org](https://nodejs.org) 下载 **Node.js 22.x LTS** 版本对应的离线安装包（`.msi`/`.pkg`），拷贝到 U 盘，在客户电脑上双击安装即可，全程无需客户电脑联网。**版本必须是 22.x**，见 §2.1 的原生模块绑定说明。

**第四步：启动系统**
- **Windows**：双击项目文件夹里的 `start-windows.bat`。
- **macOS / Linux**：双击（或在终端执行 `./start-mac-linux.sh`）项目文件夹里的 `start-mac-linux.sh`。

脚本会自动检测 Node.js 是否安装、`dist/` 是否构建完成，然后启动服务并在窗口里提示访问地址（默认 `http://localhost:3000`）。**只要这个窗口保持打开，服务就一直在运行；关闭窗口即代表服务停止。**

**第五步：打开浏览器使用**
在客户电脑上打开 Chrome/Edge 等现代浏览器，访问启动脚本提示的地址，首页登录密码见 `.env` 中的 `VITE_LOGIN_PASSWORD`。

### 3.2 单机场景的核心注意事项

- **系统本身不依赖任何在线服务**：数据持久化是本地 SQLite 模式，不需要连接任何数据库服务器或云服务；系统内不含任何需要联网的 AI 调用；页面引用的外部字体/表情图标资源均已做异步非阻塞加载，完全离线也不会卡顿或白屏。
- **数据备份需要客户自行定期做好**：由于是纯本地存储，没有任何云端自动备份，一旦客户电脑硬盘损坏或误删 `data/` 目录，数据将无法找回。系统本身有自动快照机制（每隔一段时间的保存会在 `data/backups/` 下生成一份带时间戳的 JSON 快照，最多保留最近 30 份），但这只能防止"最近误操作"，防不了硬盘物理损坏。**强烈建议客户定期（如每周）把整个 `data/` 目录（含 `kpmss.sqlite`、`kpmss.sqlite-wal`、`kpmss.sqlite-shm` 三个数据库文件，以及 `backups/` 快照目录）拷贝到 U 盘或另一台电脑做异地备份**；`-wal`/`-shm` 是数据库预写日志的一部分，务必和主文件一起拷贝，缺失会导致最近未落盘的修改丢失（详见第五章"数据备份与恢复"）。
- **局域网多人访问场景下的权限提示**：如果客户是"一台电脑当服务器、局域网内其他电脑通过浏览器访问"的用法，请注意当前登录密码校验只在浏览器前端进行，服务端接口本身没有二次鉴权——能访问到服务器所在局域网的人理论上可以绕过登录页直接调用数据接口。完全独立单机、仅本机访问时这个风险基本不存在；局域网内多人共享访问时，建议评估是否需要增加服务端鉴权（属于额外的安全加固需求）。
- **首页与管理后台密码是明文写在 `.env` 里、且会被打包进浏览器可读的前端文件中**：仅用于防止普通员工误触修改配置，不是面向攻击者设计的强安全机制，对完全离线单机场景是合理够用的，但不要当作能防御"内部人员用浏览器开发者工具查看源码"级别的安全边界。

### 3.3 常用配置（`.env` 文件）

| 配置项 | 说明 | 默认值 |
|---|---|---|
| `PORT` | 服务监听端口。如果启动时提示端口被占用，修改这一项为其他数字（如 `8080`）后重新启动即可 | `3000` |
| `VITE_LOGIN_PASSWORD` | 首页登录密码 | `guest` |
| `VITE_ADMIN_PASSWORD` | 管理后台密码 | `admin` |
| `STORAGE_TYPE` | 存储模式，离线单机部署请保持 `local` | `local` |
| `LOCAL_DATA_DIR` | 本地数据存储目录，SQLite 数据库文件（`kpmss.sqlite`）与备份快照目录（`backups/`）均位于此目录下 | `data` |
| `LOCAL_LOG_DIR` | 日志归档目录（按日期自动分文件） | `data/logs` |
| `LOG_MAX_FILE_SIZE_MB` | 单个日志文件体积上限（MB），超出后自动滚动到下一个文件 | `5` |

修改 `.env` 后需要**重新启动**（关闭启动窗口再重新运行启动脚本）才能生效。

---

## 四、方式 B：云服务器部署

### 4.1 服务器选型建议

系统本身很轻量（Node.js 单进程 + SQLite/COS），对配置要求很低：

- **配置**：1 核 2GB 内存起步即可满足单个食堂/学校的使用量；如果预计多个食堂/多组织共用一套系统，建议 2 核 4GB 起步。
- **操作系统**：建议 Linux（如 Ubuntu 22.04/CentOS/腾讯云 OpenCloudOS 等常见发行版），x86_64 架构最通用（`better-sqlite3` 官方预编译二进制对 x86_64/arm64 Linux 均有覆盖，但 x86_64 生态更成熟、遇到问题更容易排查）。
- **磁盘**：需要一块**持久化磁盘**用来存放 `data/` 目录（本地 SQLite 模式下）——如果服务器是可能被"重建/重装"的临时实例（比如某些容器化/竞价实例场景），务必确认 `data/` 挂载在持久卷上，否则实例重建会连同数据一起丢失。

### 4.2 存储模式选择：本地 SQLite vs 腾讯云 COS

云服务器部署时，`.env` 里的 `STORAGE_TYPE` 有两个选择，各有取舍：

| | `STORAGE_TYPE=local`（本地 SQLite） | `STORAGE_TYPE=cos`（腾讯云对象存储） |
|---|---|---|
| 数据存放位置 | 服务器本地磁盘 `data/kpmss.sqlite` | 腾讯云 COS 对象存储桶 |
| 优点 | 配置简单，和单机模式完全一致，读写性能最好 | 数据不依赖单台服务器的磁盘，COS 自身多副本更耐久；服务器重建/迁移/扩容更灵活，只要重新配置 COS 密钥即可 |
| 缺点 | 依赖这台服务器的磁盘可靠性和持久化配置；服务器需要自己做定期异地备份 | 每次保存需要一次完整的对象读改写（COS 没有行级别写入 API），数据量很大时会比本地 SQLite 略慢；需要额外的腾讯云账号与费用 |
| 推荐场景 | 单台服务器长期稳定运行、有自己的备份计划 | 用云厂商托管数据可靠性、服务器可能弹性伸缩/重建的场景 |

选择 `cos` 模式时，`.env` 需额外配置：

```bash
STORAGE_TYPE="cos"
COS_SECRET_ID="你的腾讯云 SecretId"
COS_SECRET_KEY="你的腾讯云 SecretKey"
COS_REGION="ap-guangzhou"           # 换成你的 COS 桶所在地域
COS_BUCKET="your-bucket-1234567890" # 换成你的桶名（含 APPID 后缀）
COS_KEY="kitchen_db.json"           # COS 里存主数据的对象键名，一般不需要改
```

> 大部分场景推荐先用 `local` 模式起步（更简单、性能更好），只有确实需要云端多副本可靠性或服务器会频繁重建时才切到 `cos` 模式。

### 4.3 构建并上传部署包

在一台联网、且操作系统/架构与目标云服务器一致的机器上（最简单的做法是**直接在云服务器上构建**，因为云服务器本身是联网的）：

```bash
# 在云服务器上直接操作（推荐，最省心）
# 1. 安装 Node.js 22.x（以 Ubuntu 为例，用 NodeSource 官方源）
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo bash -
sudo apt-get install -y nodejs
node --version   # 确认输出 v22.x.x

# 2. 上传/拉取项目代码到服务器（用 git clone、scp、或 rsync 均可）
git clone <你的代码仓库地址> /opt/kpmss
cd /opt/kpmss

# 3. 安装依赖并构建
npm install
npm run build
```

如果不方便在云服务器上直接构建（比如离线交付的镜像场景），也可以在本地一台同架构的机器/容器里构建好，再用 `scp`/`rsync` 把 `dist/`、`node_modules/`、`package.json`、`.env`、`data/`（如有）整体传到服务器：

```bash
rsync -avz --exclude='.git' --exclude='src' --exclude='server' \
  ./ user@your-server-ip:/opt/kpmss/
```

### 4.4 安装 Node.js 22.x（如果没有在服务器上直接构建）

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo bash -
sudo apt-get install -y nodejs
node --version
```

（CentOS/RHEL 系发行版把 `apt-get` 换成 `yum`/`dnf`，NodeSource 官网有对应脚本；也可以用 `nvm install 22` 管理版本。）

### 4.5 进程守护：让服务开机自启、崩溃自动重启

云服务器不像单机部署那样有人盯着终端窗口，必须用进程守护工具让服务持续运行。推荐 **pm2**（Node 生态最常用，简单够用）：

```bash
# 全局安装 pm2（只需一次）
sudo npm install -g pm2

# 启动服务并交给 pm2 管理
cd /opt/kpmss
pm2 start dist/server.cjs --name kpmss

# 设置开机自启（会输出一条 sudo 命令，按提示复制执行一次即可）
pm2 startup
pm2 save

# 常用运维命令
pm2 status              # 查看运行状态
pm2 logs kpmss           # 实时查看日志
pm2 restart kpmss        # 重启（改完 .env 后需要这一步生效）
pm2 stop kpmss           # 停止
```

如果偏好用 `systemd`（更贴近 Linux 原生方式），可以创建 `/etc/systemd/system/kpmss.service`：

```ini
[Unit]
Description=食堂用餐服务管理系统
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/kpmss
ExecStart=/usr/bin/node dist/server.cjs
Restart=on-failure
RestartSec=5
User=www-data

[Install]
WantedBy=multi-user.target
```

然后：
```bash
sudo systemctl daemon-reload
sudo systemctl enable --now kpmss
sudo systemctl status kpmss
sudo journalctl -u kpmss -f   # 查看实时日志
```

两种方式选一种即可，pm2 上手更快，systemd 更贴近系统原生运维习惯。

### 4.6 配置 `.env`（云服务器场景务必检查这几项）

```bash
PORT="3000"                      # 建议只监听内网/本机，前面套一层反向代理（见 4.7）
VITE_LOGIN_PASSWORD="改成强密码" # ⚠️ 务必修改默认值！
VITE_ADMIN_PASSWORD="改成强密码" # ⚠️ 务必修改默认值！
STORAGE_TYPE="local"             # 或 "cos"，见 4.2
LOCAL_DATA_DIR="data"
LOCAL_LOG_DIR="data/logs"
LOG_MAX_FILE_SIZE_MB="5"
```

**云服务器场景下，`VITE_LOGIN_PASSWORD`/`VITE_ADMIN_PASSWORD` 必须改成非默认的强密码**——单机离线场景下默认密码"够用"是因为攻击者接触不到这台机器，但云服务器一旦暴露公网 IP，默认密码 `guest`/`admin` 会在几分钟内被自动化扫描工具尝试到。改完 `.env` 后记得重启服务（`pm2 restart kpmss` 或 `systemctl restart kpmss`）。

### 4.7 反向代理 + HTTPS（强烈建议）

系统自身（Express）不处理 HTTPS/TLS，也没有做请求速率限制。云服务器暴露公网时，推荐在前面套一层 **Nginx** 反向代理，由 Nginx 负责 HTTPS 终结、只让应用自身监听 `127.0.0.1`（不直接对公网暴露应用端口）：

```bash
# 安装 nginx 和免费证书工具 certbot（以 Ubuntu 为例）
sudo apt-get install -y nginx certbot python3-certbot-nginx
```

Nginx 配置示例（`/etc/nginx/sites-available/kpmss`）：

```nginx
server {
    listen 80;
    server_name your-domain.com;   # 换成你的域名，没有域名可以先只用 IP + 80 端口跑起来

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/kpmss /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx

# 有域名的话，一键签发并自动续期 HTTPS 证书
sudo certbot --nginx -d your-domain.com
```

没有域名、只想用 IP 临时访问的场景，可以先跳过 HTTPS 直接用 `http://服务器IP` 访问（走 Nginx 的 80 端口），但**不建议长期这样对外提供服务**，尤其涉及密码登录的场景，明文 HTTP 传输密码有被局域网/中间节点窃听的风险。

### 4.8 防火墙 / 安全组设置

云服务商控制台的"安全组"（或服务器自身的 `ufw`/`firewalld`）只需放行：
- `80`（HTTP，用于 Nginx 转发 / certbot 验证）
- `443`（HTTPS，配置好证书后）
- `22`（SSH 管理用，建议进一步限制只允许你自己的公网 IP 访问）

**应用自身监听的端口（`.env` 里的 `PORT`，默认 3000）不需要、也不应该在安全组里对公网放行**——它应该只监听 `127.0.0.1`，由 Nginx 代理转发，避免绕开 Nginx 直接访问到裸的 Express 服务。

### 4.9 验证访问

```bash
# 服务器本机验证进程是否正常监听
curl http://127.0.0.1:3000/api/health
# 期望输出：{"status":"alive","timestamp":"..."}
```

浏览器访问 `https://your-domain.com`（或 `http://服务器IP`），应该能看到登录页。

### 4.10 云服务器场景专属安全加固清单 ⚠️

系统的登录密码校验目前**只在浏览器前端做**，后端接口本身没有二次鉴权（这个设计对单机离线场景是合理的，因为攻击者根本接触不到那台机器；但云服务器一旦暴露公网 IP，情况完全不同）。上线到公网前请务必确认以下几项：

1. **改掉默认密码**（见 §4.6），不要用 `guest`/`admin`。
2. **认识到接口本身无鉴权**：任何知道你服务器地址的人，理论上都可以绕过登录页直接调用 `/api/storage/load`、`/api/storage/save` 等接口读写数据。根据实际敏感程度，建议至少做到以下之一：
   - 在 Nginx 层加一道 HTTP Basic Auth（`auth_basic` 指令 + `htpasswd` 文件），作为在应用自身鉴权之前的一道网络层防线；
   - 通过安全组/防火墙做 IP 白名单，只允许已知的办公网络出口 IP 访问；
   - 如果只是内部几个人用，考虑整个服务只挂在内网/VPN 之后，不直接暴露公网 IP；
   - 更彻底的方案是给后端接口加真正的会话鉴权（这属于代码改动，需要单独评估排期，不在本手册范围内）。
3. **务必配置 HTTPS**（见 §4.7），避免密码和数据明文在公网传输。
4. **`.env` 文件不能被静态托管暴露**：确认 Nginx/应用不会把 `.env`、`data/` 目录当作静态文件对外提供下载（本系统的 Express 静态托管只指向 `dist/`，正常配置下不会有这个问题，但如果之后有人改了托管根目录配置，务必重新确认）。
5. **数据备份**：即使选择本地 SQLite 模式，也建议配置定期异地备份（见下方 §4.11），不要完全依赖单台云服务器的磁盘。

### 4.11 数据备份策略

**本地 SQLite 模式**：写一个定时任务（`crontab`），定期把整个 `data/` 目录（含 `kpmss.sqlite`/`kpmss.sqlite-wal`/`kpmss.sqlite-shm`/`backups/`）打包并同步到别处（另一台服务器、对象存储、或云服务商的快照服务）：

```bash
# 编辑 crontab
crontab -e

# 每天凌晨 3 点打包备份到 /backup 目录（示例，可再接一步 scp/rclone 同步到异地）
0 3 * * * tar -czf /backup/kpmss-data-$(date +\%Y\%m\%d).tar.gz -C /opt/kpmss data
```

也可以直接使用云服务商的**磁盘快照**功能对整台服务器或数据盘定期打快照，作为额外的一层保险。

**COS 模式**：数据本身已经在腾讯云对象存储里，天然具备云厂商的多副本可靠性，可以视需要再额外配置 COS 的版本控制/跨地域复制作为更进一步的保险，一般不需要再手工做本地备份。

### 4.12 更新 / 重新部署流程（不丢数据）

```bash
cd /opt/kpmss

# 1. 停止服务
pm2 stop kpmss    # 或 sudo systemctl stop kpmss

# 2. 更新代码（git pull 或替换新的 dist/、node_modules/）
git pull
npm install
npm run build

# 3. 重新启动
pm2 restart kpmss # 或 sudo systemctl start kpmss
```

**`data/` 目录始终不受影响**——只要不手动删除它，更新代码、重新构建、重启服务都不会碰到已有数据。

### 4.13 云服务器场景常见问题排查

- **`curl http://127.0.0.1:3000/api/health` 没有响应**：先看 `pm2 logs kpmss` 或 `journalctl -u kpmss -f` 有没有报错，常见原因是端口被占用（改 `.env` 的 `PORT`）或 Node 版本不对（见下一条）。
- **启动报错 `ERR_DLOPEN_FAILED` / `NODE_MODULE_VERSION` 不匹配**：说明构建部署包时用的 Node 大版本和服务器实际运行的 Node 大版本不一致。解决办法：在服务器上确认 `node --version` 是 22.x，然后 `rm -rf node_modules && npm install && npm run build` 用服务器自己的 Node 重新构建一次。
- **浏览器能访问但一直转圈/接口 404**：检查 Nginx 的 `proxy_pass` 地址端口是否和 `.env` 里的 `PORT` 一致；`sudo nginx -t` 检查配置语法；`sudo systemctl reload nginx` 应用配置。
- **HTTPS 证书到期**：`certbot` 默认会安装自动续期的定时任务，可以用 `sudo certbot renew --dry-run` 测试续期流程是否正常。
- **服务器重启后系统没有自动拉起**：确认执行过 `pm2 startup && pm2 save`（pm2 方式）或 `systemctl enable kpmss`（systemd 方式）。

---

## 五、日常使用与运维操作手册

以下内容对两种部署方式都适用。

### 5.1 登录与访问

- 首页需要输入 `VITE_LOGIN_PASSWORD` 对应的密码才能进入系统主界面。
- 点击顶部"进入管理后台"需要另外输入 `VITE_ADMIN_PASSWORD` 对应的密码，管理后台用于维护一级人群分类、二级食材大类、原料字典、以及查看系统运行日志。

### 5.2 核心功能入口

- **台账数据录入**：左侧"原料购销台账"，支持按台账（人群）、按日期录入采购入库/出库明细，"开启今日录入"后编辑，点击"保存并同步今日采购"提交。
- **备餐细表**：主界面按品类（蔬菜/粮油/调料/肉类/低耗品/水果等）查看与编辑每日备餐用量，支持"EXCEL 日历总矩阵"和"单日聚焦卡片"两种视图切换。
- **打印与导出**：支持打印记账登记表（预览后确认打印），支持导出本月细表 CSV。
- **管理后台**：一二级分类维护、原料字典维护、备份/恢复入口、系统日志查看。

### 5.3 数据备份与恢复（系统内置功能）

系统会自动生成 JSON 格式的历史快照（保存在 `data/backups/` 下，节流生成、最多保留 30 份），可以在管理后台的备份管理界面查看快照列表并选择恢复到某个历史时间点。**注意**：这类快照只覆盖台账/报表/字典等"骨架"配置数据，**不包含台账逐日出入库流水明细**（这是从最早版本延续至今的既有设计，逐日流水以数据库文件本身为准，不在快照恢复范围内）。

如果需要完整、包含逐日流水在内的数据副本（比如迁移到另一台机器、或做灾难恢复演练），必须直接复制 `data/kpmss.sqlite`、`data/kpmss.sqlite-wal`、`data/kpmss.sqlite-shm` 三个文件（务必三个一起拷贝，缺 `-wal`/`-shm` 会丢失最近尚未合并进主文件的修改），最稳妥的做法是先停止服务再拷贝，避免拷贝到写入过程中的不一致状态。

### 5.4 修改配置

改动 `.env` 里的任何配置项后，都需要**重启服务**才能生效：
- 单机部署：关闭启动脚本窗口，重新双击启动脚本。
- 云服务器部署：`pm2 restart kpmss` 或 `sudo systemctl restart kpmss`。

### 5.5 查看运行日志

- 应用日志：`data/logs/` 目录下按天分文件（如 `app-2026-07-05.log`），超过 `LOG_MAX_FILE_SIZE_MB` 会自动滚动到下一个分片。
- 进程/控制台日志：单机部署直接看启动窗口输出；云服务器部署用 `pm2 logs kpmss` 或 `journalctl -u kpmss -f`。
- 管理后台内也有一个进程通信与性能监控日志面板，可以在系统运行中实时查看客户端上报的日志。

### 5.6 停止 / 重启服务

| 部署方式 | 停止 | 重启 |
|---|---|---|
| 客户单机 | 关闭启动脚本窗口 | 重新双击启动脚本 |
| 云服务器（pm2） | `pm2 stop kpmss` | `pm2 restart kpmss` |
| 云服务器（systemd） | `sudo systemctl stop kpmss` | `sudo systemctl restart kpmss` |

---

## 六、通用常见问题 FAQ

- **提示找不到 `dist/server.cjs`**：说明构建产物没有正确生成或没有拷贝完整，回到第二章重新执行 `npm install && npm run build`。
- **提示端口被占用（`EADDRINUSE`）**：修改 `.env` 里的 `PORT` 为其他端口号后重启。
- **启动报错 `ERR_DLOPEN_FAILED` / `NODE_MODULE_VERSION` 不匹配**：构建部署包的机器和实际运行的机器 Node.js 大版本不一致，见 §2.1；解决办法是在与运行环境相同 Node 大版本（22.x）、相同操作系统/架构的机器上重新执行一次 `npm install && npm run build`。
- **数据似乎"丢失"了**：先检查是不是选错了日期/受众分组，本系统很多数据按日期归档展示，不同日期查看内容不同，并非真的丢失；确需恢复历史配置数据可在管理后台或 `data/backups/` 目录下找最近的自动快照（注意 §5.3 提到的"快照不含逐日流水"的限制）。
- **浏览器打不开页面**：单机部署确认启动窗口里显示了 `[KITCHEN SYSTEM LIVE] 服务就绪!` 字样；云服务器部署确认 Nginx/进程守护均正常运行，端口和 `.env` 配置一致。
